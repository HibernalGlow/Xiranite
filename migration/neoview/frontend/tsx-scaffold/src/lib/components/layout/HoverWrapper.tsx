/**
 * @migrated-from src/lib/components/layout/HoverWrapper.svelte
 * @source-hash sha256:5d0e6929622519e0b179067398aa0900c9a8b97def1bb53ef783590744b4d158
 * @features panels-toolbar-shell,startup-window-cli-lifecycle,theme-background-empty-state
 * @source-disposition manual
 * @migration-status partial-scaffold
 * @unsupported Svelte runtime import; attribute BindDirective; template node RenderTag
 * Generated by @xiranite/svelte-migrate; move to production only after behavior tests pass.
 */
import { Fragment } from "react"
interface Props {
		isVisible: boolean;
		pinned: boolean;
		onVisibilityChange: (visible: boolean) => void;
		children: Snippet;
		hideDelay?: number;
		showDelay?: number;
		side?: 'left' | 'right'; // 显式指定侧边栏位置
	}

export function HoverWrapper(props: Props) {
  const {
		isVisible = $bindable(false),
		pinned,
		onVisibilityChange,
		children,
		hideDelay = 500, // 增加到 500ms 提高容错性
		showDelay = 0,
		side
	} = props
  let hideTimer: number | null = null;

  let showTimer: number | null = null;

  let isContextMenuOpen = $state(false);

  let wrapperContainer = $state<HTMLDivElement | null>(null);

  let lastX = 0;

  function isInputting(): boolean {
      const el = document.activeElement;
      if (!el) return false;
      const tag = el.tagName.toLowerCase();
      return tag === 'input' || tag === 'textarea' || (el as HTMLElement).isContentEditable;
    }

  function isFloatingElement(element: Element | null): boolean {
      if (!element) return false;
      if (element.parentElement === document.body) return true;

      const style = window.getComputedStyle(element);
      if (style.position === 'fixed' && parseInt(style.zIndex) >= 50) return true;

      const floatingSelectors = [
        '[role="tooltip"]',
        '[role="popover"]',
        '[role="menu"]',
        '[data-radix-popper-content-wrapper]',
        '.tooltip',
        '.popover'
      ];
      return floatingSelectors.some((selector) => {
        try {
          return element.matches(selector) || element.closest(selector);
        } catch {
          return false;
        }
      });
    }

  $effect(() => {
      if (isVisible && !pinned && !isContextMenuOpen) {
        const checkMousePosition = (e: MouseEvent) => {
          if (!wrapperContainer) return;

          const { clientX, clientY } = e;
          const dx = clientX - lastX;
          lastX = clientX;

          const rect = wrapperContainer.getBoundingClientRect();
          // 优先使用显式指定的 side，否则回退到基于位置的猜测（仅作兼容）
          const isLeftSide = side ? side === 'left' : rect.left < window.innerWidth / 2;

          // 1. 如果鼠标在容器内，或正在输入，或在 Tooltip 上，绝对保持开启
          const elementAtMouse = document.elementFromPoint(clientX, clientY);
          if (
            (wrapperContainer && wrapperContainer.contains(elementAtMouse)) ||
            isInputting() ||
            isFloatingElement(elementAtMouse)
          ) {
            resetHideTimer();
            return;
          }

          // 2. 智能滑回逻辑：如果鼠标正在向侧边栏方向移动，重置收回计时
          if ((isLeftSide && dx < -1) || (!isLeftSide && dx > 1)) {
            resetHideTimer();
            return;
          }

          // 3. 动态物理红线判定：
          // 左侧边栏红线：取 容器宽度 + 10px
          // 右侧边栏红线：取 容器左沿 - 10px
          if (isLeftSide) {
            const retractLine = rect.right + 10;
            if (clientX > retractLine) {
              performHide();
            } else {
              resetHideTimer();
            }
          } else {
            const retractLine = rect.left - 10;
            if (clientX < retractLine) {
              performHide();
            } else {
              resetHideTimer();
            }
          }
        };

        window.addEventListener('mousemove', checkMousePosition);
        return () => window.removeEventListener('mousemove', checkMousePosition);
      }
    });

  function resetHideTimer() {
      if (hideTimer) {
        clearTimeout(hideTimer);
        hideTimer = null;
      }
    }

  function performHide() {
      if (hideTimer || isContextMenuOpen) return;
      hideTimer = setTimeout(() => {
        if (!isContextMenuOpen && !isInputting()) {
          isVisible = false;
          onVisibilityChange?.(false);
        }
        hideTimer = null;
      }, hideDelay) as unknown as number;
    }

  function handleMouseEnter() {
      if (!pinned) {
        resetHideTimer();
        if (showTimer) {
          clearTimeout(showTimer);
          showTimer = null;
        }
        const delay = showDelay ?? 0;
        if (!isVisible && delay > 0) {
          showTimer = setTimeout(() => {
            isVisible = true;
            onVisibilityChange?.(true);
          }, delay) as unknown as number;
        } else {
          isVisible = true;
          onVisibilityChange?.(true);
        }
      }
    }

  function handleMouseLeave() {
      /* 由物理判定取代 */
    }

  function handleContextMenu(e: MouseEvent) {
      if (!pinned) {
        isContextMenuOpen = true;
        resetHideTimer();
      }
    }

  function handleContextMenuClose() {
      if (isContextMenuOpen) isContextMenuOpen = false;
    }

  $effect(() => {
      if (pinned) {
        isVisible = true;
        resetHideTimer();
        onVisibilityChange?.(true);
      }
    });

  $effect(() => {
      return () => {
        if (hideTimer) clearTimeout(hideTimer);
        if (showTimer) clearTimeout(showTimer);
      };
    });
  return (
    <Fragment>

<div className="relative flex h-full w-full" data-hover-wrapper="true" role="button" tabIndex="0" onMouseEnter={handleMouseEnter} onMouseLeave={handleMouseLeave} onContextmenu={handleContextMenu} onClick={handleContextMenuClose} onKeyDown={(e) => {
		if (e.key === 'Enter' || e.key === ' ') {
			e.preventDefault();
			handleContextMenuClose();
		}
	}}>
	{/* TODO unsupported RenderTag */}
</div></Fragment>
  )
}
