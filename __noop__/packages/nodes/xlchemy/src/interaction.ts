import type { InteractionField, InteractionValues, TerminalInteractionSchema } from "@xiranite/cli-runtime/interaction"
import { DEFAULT_RAM_OPTIMIZER_RULES } from "./ram-optimizer.js"
import type { XlchemyAction, XlchemyExistingPolicy, XlchemyFormat, XlchemyInput, XlchemyResult } from "./core.js"

export type XlchemyInteractionValues = InteractionValues & { action: XlchemyAction; pathsText: string; format: XlchemyFormat }

export function createXlchemyInteractionSchema(defaults: Partial<XlchemyInteractionValues> = {}, language: "zh" | "en" = "zh"): TerminalInteractionSchema<XlchemyInput, XlchemyResult> {
  const zh = language === "zh", text = (cn: string, en: string) => zh ? cn : en
  const isFormat = (...formats: XlchemyFormat[]) => (values: Readonly<InteractionValues>) => formats.includes(String(values.format) as XlchemyFormat)
  const is = (id: string, expected: string | boolean) => (values: Readonly<InteractionValues>) => values[id] === expected
  const and = (...checks: Array<(values: Readonly<InteractionValues>) => boolean>) => (values: Readonly<InteractionValues>) => checks.every((check) => check(values))
  const jxl = isFormat("JPEG XL", "Lossless JPEG Transcoding")
  const avif = isFormat("AVIF"), jpeg = isFormat("JPEG"), smallest = isFormat("Smallest Lossless")
  const downscale = is("downscaleEnabled", true), custom = is("enableCustomArgs", true), dynamicRam = is("ramOptimizer", "dynamic")
  const initialValues: XlchemyInteractionValues = {
    action: "plan", pathsText: "", format: "JPEG XL", lossless: false, quality: 60, effort: 7, maxCompression: false, threads: 4,
    outputMode: "source", outputDir: "", existingPolicy: "skip", preserveMetadata: true, preserveStructure: true, preserveTimestamps: false, recursive: true,
    deleteOriginal: false, deleteOriginalMode: "trash", intelligentEffort: false, jxlModular: false, jxlVerify: false, jxlPngFallback: true, jxlNormalize: false, jxlNormalizeWhen: "on-fail",
    chromaSubsampling: "default", keepIfLarger: false, copyIfLarger: false, smallestPng: true, smallestWebp: true, smallestJxl: true,
    jpegEncoder: "jpegli", avifEncoder: "aom", avifBitDepth: "auto", avifAomIqTune: false, disableProgressiveJpegli: false, autoLosslessJpeg: true,
    processingOrder: "original", excludedFormatsText: "avif,jxl,webp,gif", metadataMode: "encoder-preserve",
    downscaleEnabled: false, downscaleMode: "resolution", downscaleWidth: 1920, downscaleHeight: 1080, downscalePercent: 50, downscaleFileSizeKb: 500, downscaleShortestSide: 1080, downscaleLongestSide: 1920, downscaleMegapixels: 2.1, downscaleResample: "default",
    enableCustomArgs: false, cjxlArgs: "", avifencArgs: "", cjpegliArgs: "", imageMagickArgs: "",
    ramOptimizer: "dynamic", ramOptimizerRules: DEFAULT_RAM_OPTIMIZER_RULES,
    exiftoolWipeArgs: '-overwrite_original -all= --ICC_Profile:all "$dst"', exiftoolPreserveArgs: '-overwrite_original -TagsFromFile "$src" -all:all "$dst"', exiftoolUnsafeWipeArgs: '-overwrite_original -all= "$dst"', exiftoolCustomArgs: '"$dst"',
    ...defaults,
  }
  const fields: InteractionField[] = [
    { id: "action", label: text("操作", "Action"), kind: "select", role: "action", options: [{ value: "plan", label: text("预览计划", "Plan") }, { value: "convert", label: text("执行转换", "Convert") }, { value: "diagnose", label: text("检测工具链", "Diagnose toolchain") }] },
    { id: "pathsText", label: text("输入文件或文件夹", "Input files or folders"), kind: "path-list", lines: 5, placeholder: text("每行一个本地路径", "One local path per line") },
    { id: "format", label: text("目标格式", "Target format"), kind: "select", options: ["JPEG XL", "AVIF", "WebP", "PNG", "TIFF", "JPEG", "Lossless JPEG Transcoding", "JPEG Reconstruction", "Smallest Lossless"].map((value) => ({ value, label: value })) },
    { id: "lossless", label: text("无损", "Lossless"), kind: "boolean", visibleWhen: isFormat("JPEG XL", "AVIF", "WebP", "PNG", "TIFF", "Smallest Lossless") },
    { id: "quality", label: text("质量", "Quality"), kind: "number", min: 1, max: 100, step: 1, visibleWhen: isFormat("JPEG XL", "AVIF", "WebP", "TIFF", "JPEG") },
    { id: "effort", label: text("压缩力度", "Effort"), kind: "number", min: 1, max: 10, step: 1, visibleWhen: isFormat("JPEG XL", "AVIF", "WebP", "PNG", "TIFF", "Lossless JPEG Transcoding") },
    { id: "maxCompression", label: text("最大压缩", "Maximum compression"), kind: "boolean", visibleWhen: jxl },
    { id: "threads", label: text("并行线程", "Threads"), kind: "number", min: 1, max: 64, step: 1 },
    { id: "jpegEncoder", label: text("JPEG 编码器", "JPEG encoder"), kind: "select", options: [{ value: "jpegli", label: "JPEGli" }, { value: "libjpeg", label: "libjpeg" }], visibleWhen: jpeg },
    { id: "avifEncoder", label: text("AVIF 编码器", "AVIF encoder"), kind: "select", options: [{ value: "aom", label: "AOM AV1" }, { value: "svt", label: "SVT-AV1-PSY" }, { value: "slimg", label: "slimg CFFI" }], visibleWhen: avif },
    { id: "avifBitDepth", label: text("AVIF 位深", "AVIF bit depth"), kind: "select", options: ["auto", "8", "10", "12"].map((value) => ({ value, label: value })), visibleWhen: avif },
    { id: "avifAomIqTune", label: text("AOM IQ 调优", "AOM IQ tune"), kind: "boolean", visibleWhen: and(avif, is("avifEncoder", "aom")) },
    { id: "disableProgressiveJpegli", label: text("禁用渐进 JPEGli", "Disable progressive JPEGli"), kind: "boolean", visibleWhen: and(jpeg, is("jpegEncoder", "jpegli")) },
    { id: "autoLosslessJpeg", label: text("JPEG 自动无损转码", "Automatic lossless JPEG"), kind: "boolean", visibleWhen: jxl },
    { id: "intelligentEffort", label: text("智能压缩力度", "Intelligent effort"), kind: "boolean", visibleWhen: jxl },
    { id: "jxlModular", label: text("JXL 有损 Modular", "JXL lossy Modular"), kind: "boolean", visibleWhen: isFormat("JPEG XL") },
    { id: "jxlVerify", label: text("JXL 完整性校验", "JXL integrity verification"), kind: "boolean", visibleWhen: jxl },
    { id: "jxlPngFallback", label: text("JXL PNG 回退", "JXL PNG fallback"), kind: "boolean", visibleWhen: jxl },
    { id: "jxlNormalize", label: text("JXL 编码前标准化", "Normalize before JXL"), kind: "boolean", visibleWhen: jxl },
    { id: "jxlNormalizeWhen", label: text("标准化时机", "Normalization timing"), kind: "select", options: [{ value: "on-fail", label: text("失败时", "On failure") }, { value: "always", label: text("始终", "Always") }], visibleWhen: and(jxl, is("jxlNormalize", true)) },
    { id: "chromaSubsampling", label: text("色度采样", "Chroma subsampling"), kind: "select", options: ["default", "444", "422", "420"].map((value) => ({ value, label: value })), visibleWhen: isFormat("AVIF", "JPEG") },
    { id: "smallestPng", label: "PNG", kind: "boolean", visibleWhen: smallest }, { id: "smallestWebp", label: "WebP", kind: "boolean", visibleWhen: smallest }, { id: "smallestJxl", label: "JXL", kind: "boolean", visibleWhen: smallest },
    { id: "outputMode", label: text("输出位置", "Output location"), kind: "select", options: [{ value: "source", label: text("源文件旁", "Beside source") }, { value: "directory", label: text("指定目录", "Directory") }] },
    { id: "outputDir", label: text("输出目录", "Output directory"), kind: "text", visibleWhen: is("outputMode", "directory") },
    { id: "existingPolicy", label: text("同名文件", "Existing file"), kind: "select", options: [{ value: "skip", label: text("跳过", "Skip") }, { value: "rename", label: text("自动改名", "Rename") }, { value: "replace", label: text("覆盖", "Replace") }] },
    { id: "preserveStructure", label: text("保留目录结构", "Preserve directory structure"), kind: "boolean" }, { id: "preserveTimestamps", label: text("保留时间戳", "Preserve timestamps"), kind: "boolean" }, { id: "recursive", label: text("递归扫描", "Recursive scan"), kind: "boolean" },
    { id: "deleteOriginal", label: text("转换后删除原图", "Delete original after conversion"), kind: "boolean" },
    { id: "deleteOriginalMode", label: text("删除方式", "Deletion mode"), kind: "select", options: [{ value: "trash", label: text("移到回收站", "Recycle bin") }, { value: "permanent", label: text("永久删除", "Permanent") }], visibleWhen: is("deleteOriginal", true) },
    { id: "keepIfLarger", label: text("保留较大结果", "Keep output if larger"), kind: "boolean" }, { id: "copyIfLarger", label: text("较大时复制原图", "Copy original if larger"), kind: "boolean" },
    { id: "processingOrder", label: text("处理顺序", "Processing order"), kind: "select", options: [["original", text("原始顺序", "Original")], ["path-asc", text("路径升序", "Path ascending")], ["path-desc", text("路径降序", "Path descending")], ["size-asc", text("大小升序", "Size ascending")], ["size-desc", text("大小降序", "Size descending")], ["random", text("随机", "Random")], ["sequential", text("顺序", "Sequential")]].map(([value, label]) => ({ value, label })) },
    { id: "excludedFormatsText", label: text("排除输入格式", "Excluded input formats"), kind: "text", placeholder: "avif,jxl,webp,gif" },
    { id: "preserveMetadata", label: text("保留元数据", "Preserve metadata"), kind: "boolean" },
    { id: "metadataMode", label: text("元数据策略", "Metadata policy"), kind: "select", options: [{ value: "encoder-wipe", label: text("编码器清除", "Encoder wipe") }, { value: "encoder-preserve", label: text("编码器保留", "Encoder preserve") }, { value: "exiftool-wipe", label: "ExifTool wipe" }, { value: "exiftool-preserve", label: "ExifTool preserve" }, { value: "exiftool-unsafe-wipe", label: "ExifTool unsafe wipe" }, { value: "exiftool-custom", label: "ExifTool custom" }] },
    { id: "exiftoolWipeArgs", label: "ExifTool wipe args", kind: "multiline", lines: 2 }, { id: "exiftoolPreserveArgs", label: "ExifTool preserve args", kind: "multiline", lines: 2 }, { id: "exiftoolUnsafeWipeArgs", label: "ExifTool unsafe wipe args", kind: "multiline", lines: 2 }, { id: "exiftoolCustomArgs", label: "ExifTool custom args", kind: "multiline", lines: 2 },
    { id: "downscaleEnabled", label: text("启用缩小", "Enable downscaling"), kind: "boolean" },
    { id: "downscaleMode", label: text("缩小模式", "Downscale mode"), kind: "select", options: [["resolution", text("分辨率", "Resolution")], ["percent", text("百分比", "Percent")], ["file-size", text("目标文件大小", "Target file size")], ["shortest-side", text("最短边", "Shortest side")], ["longest-side", text("最长边", "Longest side")], ["megapixels", text("百万像素", "Megapixels")]].map(([value, label]) => ({ value, label })), visibleWhen: downscale },
    { id: "downscaleWidth", label: text("缩小宽度", "Width"), kind: "number", min: 1, max: 32768, visibleWhen: and(downscale, is("downscaleMode", "resolution")) }, { id: "downscaleHeight", label: text("缩小高度", "Height"), kind: "number", min: 1, max: 32768, visibleWhen: and(downscale, is("downscaleMode", "resolution")) },
    { id: "downscalePercent", label: text("百分比", "Percent"), kind: "number", min: 1, max: 1000, visibleWhen: and(downscale, is("downscaleMode", "percent")) }, { id: "downscaleFileSizeKb", label: text("目标 KB", "Target KB"), kind: "number", min: 1, visibleWhen: and(downscale, is("downscaleMode", "file-size")) },
    { id: "downscaleShortestSide", label: text("最短边", "Shortest side"), kind: "number", min: 1, visibleWhen: and(downscale, is("downscaleMode", "shortest-side")) }, { id: "downscaleLongestSide", label: text("最长边", "Longest side"), kind: "number", min: 1, visibleWhen: and(downscale, is("downscaleMode", "longest-side")) }, { id: "downscaleMegapixels", label: text("百万像素", "Megapixels"), kind: "number", min: 0.01, step: 0.1, visibleWhen: and(downscale, is("downscaleMode", "megapixels")) },
    { id: "downscaleResample", label: text("重采样", "Resampling"), kind: "select", options: ["default", "lanczos", "mitchell", "catrom", "box"].map((value) => ({ value, label: value })), visibleWhen: downscale },
    { id: "ramOptimizer", label: text("内存优化器", "RAM optimizer"), kind: "select", options: [{ value: "dynamic", label: text("动态规则", "Dynamic") }, { value: "static", label: text("静态单任务", "Static") }, { value: "disabled", label: text("关闭", "Disabled") }] },
    { id: "ramOptimizerRules", label: text("内存优化规则", "RAM optimizer rules"), kind: "multiline", lines: 3, visibleWhen: dynamicRam },
    { id: "enableCustomArgs", label: text("启用额外编码参数", "Enable custom encoder arguments"), kind: "boolean" },
    { id: "cjxlArgs", label: "cjxl args", kind: "multiline", lines: 2, visibleWhen: custom }, { id: "avifencArgs", label: "avifenc args", kind: "multiline", lines: 2, visibleWhen: custom }, { id: "cjpegliArgs", label: "cjpegli args", kind: "multiline", lines: 2, visibleWhen: custom }, { id: "imageMagickArgs", label: "ImageMagick args", kind: "multiline", lines: 2, visibleWhen: custom },
  ]
  return {
    id: "xlchemy", title: "Xlchemy", description: text("批量发现、规划并转换图片。", "Discover, plan, and transcode images in batches."), initialValues, fields,
    view: { sections: [
      { id: "input", title: text("输入与转换", "Input and conversion"), fieldIds: ["action", "pathsText", "format", "lossless", "quality", "effort", "maxCompression", "threads", "jpegEncoder", "avifEncoder", "avifBitDepth", "avifAomIqTune", "disableProgressiveJpegli", "autoLosslessJpeg", "intelligentEffort", "jxlModular", "jxlVerify", "jxlPngFallback", "jxlNormalize", "jxlNormalizeWhen", "chromaSubsampling", "smallestPng", "smallestWebp", "smallestJxl"] },
      { id: "files", title: text("保存与文件", "Output and files"), fieldIds: ["outputMode", "outputDir", "existingPolicy", "preserveStructure", "preserveTimestamps", "recursive", "deleteOriginal", "deleteOriginalMode", "keepIfLarger", "copyIfLarger", "processingOrder", "excludedFormatsText"] },
      { id: "modify", title: text("缩小与元数据", "Resize and metadata"), fieldIds: ["preserveMetadata", "metadataMode", "exiftoolWipeArgs", "exiftoolPreserveArgs", "exiftoolUnsafeWipeArgs", "exiftoolCustomArgs", "downscaleEnabled", "downscaleMode", "downscaleWidth", "downscaleHeight", "downscalePercent", "downscaleFileSizeKb", "downscaleShortestSide", "downscaleLongestSide", "downscaleMegapixels", "downscaleResample"] },
      { id: "advanced", title: text("高级", "Advanced"), fieldIds: ["ramOptimizer", "ramOptimizerRules", "enableCustomArgs", "cjxlArgs", "avifencArgs", "cjpegliArgs", "imageMagickArgs"] },
    ], dashboard: { title: "Xlchemy", display: (values) => ({ primary: String(values.format), secondary: String(values.action), metrics: [{ label: text("质量", "Quality"), value: String(values.quality) }, { label: text("线程", "Threads"), value: String(values.threads) }] }) } },
    toInput: (v) => ({ action: String(v.action ?? "plan") as XlchemyAction, paths: lines(v.pathsText), format: String(v.format ?? "JPEG XL") as XlchemyFormat, lossless: v.lossless === true, quality: num(v.quality, 60), effort: num(v.effort, 7), maxCompression: v.maxCompression === true, threads: num(v.threads, 4), outputMode: String(v.outputMode ?? "source") as XlchemyInput["outputMode"], outputDir: string(v.outputDir) || undefined, existingPolicy: String(v.existingPolicy ?? "skip") as XlchemyExistingPolicy, overwrite: v.existingPolicy === "replace", preserveMetadata: v.preserveMetadata !== false, preserveStructure: v.preserveStructure !== false, preserveTimestamps: v.preserveTimestamps === true, recursive: v.recursive !== false, deleteOriginal: v.deleteOriginal === true, deleteOriginalMode: String(v.deleteOriginalMode ?? "trash") as XlchemyInput["deleteOriginalMode"], intelligentEffort: v.intelligentEffort === true, jxlModular: v.jxlModular === true, jxlVerify: v.jxlVerify === true, jxlPngFallback: v.jxlPngFallback !== false, jxlNormalize: v.jxlNormalize === true, jxlNormalizeWhen: String(v.jxlNormalizeWhen ?? "on-fail") as XlchemyInput["jxlNormalizeWhen"], chromaSubsampling: String(v.chromaSubsampling ?? "default"), keepIfLarger: v.keepIfLarger === true, copyIfLarger: v.copyIfLarger === true, smallestFormatPool: { png: v.smallestPng !== false, webp: v.smallestWebp !== false, jxl: v.smallestJxl !== false }, jpegEncoder: String(v.jpegEncoder ?? "jpegli") as XlchemyInput["jpegEncoder"], avifEncoder: String(v.avifEncoder ?? "aom") as XlchemyInput["avifEncoder"], avifBitDepth: String(v.avifBitDepth ?? "auto") as XlchemyInput["avifBitDepth"], avifAomIqTune: v.avifAomIqTune === true, disableProgressiveJpegli: v.disableProgressiveJpegli === true, autoLosslessJpeg: v.autoLosslessJpeg !== false, processingOrder: String(v.processingOrder ?? "original") as XlchemyInput["processingOrder"], excludedFormats: list(v.excludedFormatsText), metadataMode: String(v.metadataMode ?? "encoder-preserve") as XlchemyInput["metadataMode"], exiftoolWipeArgs: string(v.exiftoolWipeArgs), exiftoolPreserveArgs: string(v.exiftoolPreserveArgs), exiftoolUnsafeWipeArgs: string(v.exiftoolUnsafeWipeArgs), exiftoolCustomArgs: string(v.exiftoolCustomArgs), downscale: { enabled: v.downscaleEnabled === true, mode: String(v.downscaleMode ?? "resolution") as NonNullable<XlchemyInput["downscale"]>["mode"], width: num(v.downscaleWidth, 1920), height: num(v.downscaleHeight, 1080), percent: num(v.downscalePercent, 50), fileSizeKb: num(v.downscaleFileSizeKb, 500), shortestSide: num(v.downscaleShortestSide, 1080), longestSide: num(v.downscaleLongestSide, 1920), megapixels: num(v.downscaleMegapixels, 2.1), resample: String(v.downscaleResample ?? "default") }, ramOptimizer: String(v.ramOptimizer ?? "dynamic") as XlchemyInput["ramOptimizer"], ramOptimizerRules: string(v.ramOptimizerRules) || DEFAULT_RAM_OPTIMIZER_RULES, enableCustomArgs: v.enableCustomArgs === true, cjxlArgs: string(v.cjxlArgs), avifencArgs: string(v.avifencArgs), cjpegliArgs: string(v.cjpegliArgs), imageMagickArgs: string(v.imageMagickArgs) }),
    validate: (_values, input) => input.action === "diagnose" ? null : !input.paths.length ? text("至少输入一个图片或文件夹。", "Enter at least one image or folder.") : input.outputMode === "directory" && !input.outputDir ? text("指定目录模式需要输出目录。", "Directory mode requires an output directory.") : input.format === "Smallest Lossless" && !Object.values(input.smallestFormatPool ?? {}).some(Boolean) ? text("最小格式池至少启用一种格式。", "Enable at least one smallest-lossless format.") : null,
    preview: (input) => input.action === "diagnose" ? [text("检测 PATH 中的完整工具链，不会安装任何内容。", "Check the complete PATH toolchain without installing anything.")] : [`${text("输入", "Inputs")}: ${input.paths.length}`, `${text("格式", "Format")}: ${input.format} / ${input.lossless ? text("无损", "lossless") : `Q${input.quality}`}`, `${text("输出", "Output")}: ${input.outputMode === "source" ? text("源文件旁", "beside source") : input.outputDir}`, input.action === "plan" ? text("安全预览：不会写入文件。", "Safe plan: no files will be written.") : text("真实转换：将写入输出文件。", "Live conversion: output files will be written.")],
    isDangerous: (input) => input.action === "convert" && (input.deleteOriginal === true || input.existingPolicy === "replace"),
    dangerPrompt: (input) => ({ title: text("确认转换", "Confirm conversion"), body: input.deleteOriginal ? text("转换成功后将按选定方式处理原图。", "Original images will be handled using the selected deletion mode.") : text("同名输出会被覆盖。", "Existing outputs will be replaced."), confirmLabel: text("确认转换", "Convert") }),
    result: (result) => ({ success: result.success, message: result.message, lines: result.data?.environment?.map((tool) => `${tool.runnable ? "ready" : "missing"} · ${tool.label} · ${tool.path ?? tool.detail ?? ""}`) ?? result.data?.errors ?? [], table: { columns: [{ id: "source", label: text("输入", "Input"), width: 28 }, { id: "output", label: text("输出", "Output"), width: 32 }, { id: "status", label: text("状态", "Status"), width: 12 }], rows: (result.data?.files ?? []).map((file) => ({ source: file.sourcePath, output: file.outputPath, status: file.status })), emptyMessage: result.message } }),
  }
}

function string(value: unknown): string { return String(value ?? "").trim() }
function lines(value: unknown): string[] { return String(value ?? "").split(/\r?\n/).map((item) => item.trim()).filter(Boolean) }
function list(value: unknown): string[] { return String(value ?? "").split(/[,;\s]+/).map((item) => item.trim()).filter(Boolean) }
function num(value: unknown, fallback: number): number { const parsed = Number(value); return Number.isFinite(parsed) ? parsed : fallback }
