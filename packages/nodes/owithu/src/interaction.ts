import type{InteractionField,InteractionValues,TerminalInteractionSchema}from"@xiranite/cli-runtime/interaction";import type{TerminalLanguage}from"@xiranite/cli-runtime/i18n";import type{OwithuAction,OwithuInput,OwithuResult,RegistryHive}from"./core.js"
export type OwithuInteractionValues=InteractionValues&{action:OwithuAction;path:string;configText:string;hive:""|RegistryHive;onlyKey:string}
export function createOwithuInteractionSchema(d:Partial<OwithuInteractionValues>={},language:TerminalLanguage="zh"):TerminalInteractionSchema<OwithuInput,OwithuResult>{const zh=language==="zh",initialValues={action:"preview",path:"",configText:"",hive:"",onlyKey:"",...clean(d)}as OwithuInteractionValues,fields:InteractionField[]=[{id:"action",label:zh?"命令":"Command",kind:"select",role:"action",options:[{value:"preview",label:zh?"◉ 预览":"◉ Preview"},{value:"register",label:zh?"⊕ 注册":"⊕ Register"},{value:"unregister",label:zh?"⊖ 注销":"⊖ Unregister"}]},{id:"path",label:zh?"配置文件":"Config file",kind:"text"},{id:"configText",label:zh?"TOML 内容":"TOML content",kind:"multiline",lines:5},{id:"hive",label:"Registry Hive",kind:"select",options:[{value:"",label:zh?"配置默认":"Config default"},{value:"HKCU",label:"HKCU"},{value:"HKCR",label:"HKCR"},{value:"HKLM",label:"HKLM"}]},{id:"onlyKey",label:zh?"仅处理条目":"Only entry key",kind:"text"}];return{id:"owithu",title:"OwithU",description:zh?"Windows 右键菜单注册表拓扑管理":"Windows context-menu registry topology manager",initialValues,fields,view:{sections:[{id:"registry",title:zh?"注册表拓扑":"Registry topology",fieldIds:fields.map(x=>x.id)}],dashboard:{title:"OwithU",display:v=>({primary:String(v.path||"TOML"),secondary:String(v.hive||"config"),metrics:[]})}},toInput:v=>({action:v.action as OwithuAction,path:String(v.path??""),configText:String(v.configText??""),hive:v.hive as RegistryHive|"",onlyKey:String(v.onlyKey??"")}),validate:(_v,i)=>i.path?.trim()||i.configText?.trim()?null:zh?"请输入配置路径或粘贴 TOML。":"Enter a config path or TOML.",preview:i=>[i.path||"inline TOML",`${i.hive||"config"}${i.onlyKey?` · ${i.onlyKey}`:""}`],isDangerous:i=>i.action==="register"||i.action==="unregister",dangerPrompt:i=>({title:i.action==="register"?(zh?"确认写入注册表":"Confirm registry write"):(zh?"确认移除注册表项":"Confirm registry removal"),body:zh?"此操作会修改 Windows 右键菜单注册表。":"This changes Windows context-menu registry keys.",confirmLabel:i.action==="register"?(zh?"确认注册":"Register"):(zh?"确认注销":"Unregister")}),result:r=>({success:r.success,message:r.message,lines:r.data?[`Entries: ${r.data.entries.length}`,`Operations: ${r.data.plan.length}`,`Registered: ${r.data.registeredCount}`,`Removed: ${r.data.unregisteredCount}`]:[]})}}
const clean=(v:Record<string,unknown>)=>Object.fromEntries(Object.entries(v).filter(([,x])=>x!==undefined))
