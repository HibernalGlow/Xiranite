# Xiranite

**源缩略图（Source Thumbnail）**：由 NeoView 内容缩略图管线按本地文件或目录路径解析、生成和缓存，并通过带生命周期的鉴权 URL 提供给任意节点的共享展示能力。调用节点只持有上下文租约和不透明 URL，不读取 NeoView 数据库，也不自行实现封面提取。_Avoid_: NeoView 私有缩略图、Windows Shell 封面、ClipM 缩略图缓存

Xiranite 将可组合节点作为共享运行时中的应用能力，并允许重要节点形成独立交付物。

## 节点应用交付

**节点独立应用（Node App）**：
只承载一个节点、可脱离 Xiranite 主应用单独启动的冻结发行物。它使用节点独立窗口的界面，但不是工作区或主应用的缩小副本。
_Avoid_: standalone tray、迷你工作区

**节点独立窗口（Node Window）**：
主 Xiranite 运行期间，将一个节点实例显示在独立顶层窗口中的界面形态。它是节点独立应用的交互基线，但本身不是独立发行物。
_Avoid_: 节点独立应用

**节点应用实例（Node App Instance）**：
节点独立应用首次启动时创建的稳定状态身份。它从节点源码声明的默认状态初始化，不来源于也不依赖工作区节点实例。
_Avoid_: 工作区节点实例、临时窗口实例

**节点应用构建器（Node App Builder）**：
不依赖 Xiranite 主应用或业务后端运行、直接从源码 checkout 构建节点独立应用的仓库脚本。
_Avoid_: companion app、模块注册器、节点设置页

**节点应用状态（Node App State）**：
节点独立应用专属的组件、窗口和浏览器 UI 状态，由宿主自动持久化。它不属于节点正式配置或业务数据，节点不负责实现其存储。
_Avoid_: 节点配置、节点业务数据库

## 外部节点启动

**外部节点启动请求（External Node Launch Request）**：
由命令行或 `xiranite://` URL 表达、经统一校验后投递给一个已声明节点的版本化打开意图。它不是工作区导航，也不是节点的私有 HTTP 请求。
_Avoid_: 主程序打开、深链接参数、节点 API 调用

**外部启动声明（External Launch Declaration）**：
节点声明的可公开启动意图、目标类别、实例策略和完整宿主能力要求。未声明的节点不能由外部启动器路由。
_Avoid_: 任意节点启动、自动发现能力

**节点启动宿主（Node Launch Host）**：
按外部启动请求只装配一个目标节点及其完整声明能力的桌面会话。它省略工作区和无关节点，但不是能力降级的节点窗口。
_Avoid_: 工作区中转、精简功能模式、节点独立应用

**受管 Shell 注册（Managed Shell Registration）**：
带 Xiranite 所有权标记、可由实际注册表状态验证并只由其所有者撤销的 Windows Shell 键集合。它不包含用户或其他程序留下的同名旧键。
_Avoid_: 注册表已启用状态、通用右键项、旧项迁移

## NeoView 文件标签

**可替换标签页（Replaceable Tab）**：
普通目录标签。历史、书签和外部浏览请求可以复用该标签的浏览器会话并替换其当前目录。
_Avoid_: 普通标签、未保护标签

**受保护标签页（Protected Tab）**：
承载搜索结果或 EFU 文件列表的临时工作区。任何会替换当前列表的外部浏览请求必须新建标签；历史或书签的穿透激活由调用方回退处理。锁图标表示此策略。
_Avoid_: 固定标签、只读标签

**固定标签页（Pinned Tab）**：
用户显式固定并持久化到 NeoView 配置的目录标签。固定描述生命周期与持久化，不代表受保护；搜索和 EFU 标签不能固定。
_Avoid_: 受保护标签

## Findz v2

**库（Library）**：
一个被 Findz 管理的本地根目录，以及与其一一对应的独立索引数据库。
_Avoid_: 全局搜索库、`xiranite.db`

**压缩包（Archive）**：
库根目录下的一个顶层 ZIP 或 CBZ 文件。
_Avoid_: 压缩包成员、嵌套压缩包

**成员（Member）**：
压缩包中央目录中的一个条目；它可以是图片候选或嵌套压缩包候选。
_Avoid_: 压缩包本身

**增量变更（Incremental Change）**：
文件观察器接收并在更新索引前合并的创建、修改、删除或重命名路径事件。
_Avoid_: 全量扫描

**图片分析（Image Analysis）**：
由用户手动启动、可暂停和恢复的任务；它读取图片成员的受限前缀以记录格式、尺寸和大小密度。
_Avoid_: 基础扫描、完整图片解码

**分析修订（Analysis Revision）**：
绑定到图片分析结果的不可变解析器、读取预算和异常策略版本。
_Avoid_: 压缩包扫描版本

**异常（Anomaly）**：
相对同实际格式和像素分组而显著偏大的图片成员，或扩展名/格式不匹配等诊断状态；不是重压缩质量结论。
_Avoid_: 自动优化建议、损坏文件

**矩形图投影（Treemap Projection）**：
按查询结果和面积指标布置的嵌套矩形可视化；它不是传统树形控件。
_Avoid_: 树形列表

**任务（Task）**：
扫描、增量同步或图片分析的可持久化生命周期记录。
_Avoid_: 单次前端请求

## NeoView 输入与文件操作

**激活身份（Activation Identity）**：
一次 Reader 打开结果中不可拆分的来源身份，包含内部阅读源、用户实际激活的文件系统条目，以及从遍历根到该条目的完整有序路径。穿透只改变内部阅读源；自动展开后单独选择的子文件会形成新的激活身份，且不会丢失它在展开层级中的相邻位置。
_Avoid_: 激活根、穿透根、当前阅读源、最外层目录

**删除目标快照（Deletion Target Snapshot）**：
删除动作被触发时从激活身份或明确的 File Card 条目捕获的不可变文件系统目标；确认、切换相邻书和后续动作都不能改变它。
_Avoid_: 当前路径、动态穿透结果、删除时重新解析的目标

**界面命令绑定（UI Command Binding）**：
由固定界面操作触发、但与键盘和轮盘共享动作及后续动作语义的系统绑定；其触发来源固定，用户只配置后续动作。
_Avoid_: 直接回调、隐藏快捷键、全局动作副作用

## ClipM 与 NeoView 来源连续性

**作品身份（Work Identity）**：
一个逻辑漫画作品的持久身份，不随当前评分或展示路径变化。
_Avoid_: 文件名、路径身份

**来源身份（Source Identity）**：
可变文件名投影发生变化时，Reader 仍用来关联同一阅读来源的稳定身份。
_Avoid_: 当前路径、展示名称

**当前路径（Current Path）**：
现在用于打开来源的实际文件系统位置；它可以变化而不改变来源身份。
_Avoid_: 作品身份

**可移植评分投影（Portable Score Projection）**：
将作品当前评分带出数据库的可选 CM 文件名块与归档元数据。
_Avoid_: 主身份、权威记录

## Neoxide 界面迁移

**Neoxide**：
基于 mImageViewer 的图像、漫画与视频查看器，是本次界面复刻、国际化重构和跨平台适配的目标应用；其已有功能是迁移时需要保留的行为基准。
_Avoid_: NeoView、Xiranite 主工作区

**Neoxide 浏览会话（Neoxide Viewing Session）**：
一个客户端独立拥有的当前浏览状态，包括打开的内容、正在显示的位置、选中项和界面布局。多个会话共享文件和业务数据，各自保持可操作，不互相接管浏览状态。
_Avoid_: 远程接管、镜像桌面、全局当前页面

**Neoxide 共享续读位置（Neoxide Shared Resume Position）**：
同一本书再次打开时，各客户端共同采用的续读位置。它不强制改变其他已打开会话正在显示的页面。
_Avoid_: 各端独立续读点、同步翻页、全局当前页面
