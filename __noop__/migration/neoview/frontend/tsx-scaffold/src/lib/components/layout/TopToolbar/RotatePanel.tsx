/**
 * @migrated-from src/lib/components/layout/TopToolbar/RotatePanel.svelte
 * @source-hash sha256:c823ae6af0f8659ac7cccdaaa2eedfbbb8c8be5088b5148288bb57f3caf4acb5
 * @features panels-toolbar-shell,zoom-rotate-magnifier
 * @source-disposition manual
 * @migration-status review-scaffold
 * Generated by @xiranite/svelte-migrate; move to production only after behavior tests pass.
 */
import { Fragment } from "react"
import { Button } from '$lib/components/ui/button';
import * as Separator from '$lib/components/ui/separator';
import * as Tooltip from '$lib/components/ui/tooltip';
import { RotateCw, RotateCcw, Ban, Smartphone } from 'lucide-react';
import { settingsManager } from '$lib/settings/settingsManager';
import { rotateClockwise } from '$lib/stores';
interface Props {
		expanded: boolean;
	}

export function RotatePanel(props: Props) {
  const { expanded } = props
  let settings = $state(settingsManager.getSettings());

  settingsManager.addListener((newSettings) => {
      settings = newSettings;
    });

  let autoRotateMode = $derived(settings.view.autoRotate?.mode ?? 'none');

  function setAutoRotateMode(
      mode:
        | 'none'
        | 'left'
        | 'right'
        | 'horizontalLeft'
        | 'horizontalRight'
        | 'forcedLeft'
        | 'forcedRight'
    ) {
      settingsManager.updateNestedSettings('view', { autoRotate: { mode } });
    }
  return (
    <Fragment>

{expanded ? (<Fragment>
	<div className="border-border/50 flex flex-wrap items-center justify-center gap-1 border-t pt-1">
		<span className="text-muted-foreground mr-2 text-xs">手动旋转</span>
		<Tooltip.Root>
			<Tooltip.Trigger>
				<Button variant="ghost" size="icon" className="h-7 w-7" onClick={rotateClockwise}>
					<RotateCw className="h-3.5 w-3.5"></RotateCw>
				</Button>
			</Tooltip.Trigger>
			<Tooltip.Content>
				<p>顺时针旋转 90°</p>
			</Tooltip.Content>
		</Tooltip.Root>

		<Separator.Root orientation="vertical" className="mx-2 h-5"></Separator.Root>

		<span className="text-muted-foreground mr-2 text-xs">自动旋转</span>
		<div className="bg-muted/60 inline-flex items-center gap-0.5 rounded-full p-0.5 shadow-inner">
			<Tooltip.Root>
				<Tooltip.Trigger>
					<Button variant={autoRotateMode === 'none' ? 'default' : 'ghost'} size="icon" className="h-7 w-7 rounded-full" onClick={() => setAutoRotateMode('none')}>
						<Ban className="h-3.5 w-3.5"></Ban>
					</Button>
				</Tooltip.Trigger>
				<Tooltip.Content><p>关闭自动旋转</p></Tooltip.Content>
			</Tooltip.Root>

			<Tooltip.Root>
				<Tooltip.Trigger>
					<Button variant={autoRotateMode === 'left' ? 'default' : 'ghost'} size="icon" className="h-7 w-7 rounded-full" onClick={() => setAutoRotateMode('left')}>
						<RotateCcw className="h-3.5 w-3.5"></RotateCcw>
					</Button>
				</Tooltip.Trigger>
				<Tooltip.Content><p>纵向左旋</p></Tooltip.Content>
			</Tooltip.Root>

			<Tooltip.Root>
				<Tooltip.Trigger>
					<Button variant={autoRotateMode === 'right' ? 'default' : 'ghost'} size="icon" className="h-7 w-7 rounded-full" onClick={() => setAutoRotateMode('right')}>
						<RotateCw className="h-3.5 w-3.5"></RotateCw>
					</Button>
				</Tooltip.Trigger>
				<Tooltip.Content><p>纵向右旋</p></Tooltip.Content>
			</Tooltip.Root>
		</div>

		<Separator.Root orientation="vertical" className="mx-2 h-5"></Separator.Root>

		<span className="text-muted-foreground mr-2 text-xs">横屏</span>
		<div className="bg-muted/60 inline-flex items-center gap-0.5 rounded-full p-0.5 shadow-inner">
			<Tooltip.Root>
				<Tooltip.Trigger>
					<Button variant={autoRotateMode === 'horizontalLeft' ? 'default' : 'ghost'} size="icon" className="h-7 w-7 rounded-full" onClick={() => setAutoRotateMode('horizontalLeft')}>
						<Smartphone className="h-3.5 w-3.5 -rotate-90"></Smartphone>
					</Button>
				</Tooltip.Trigger>
				<Tooltip.Content><p>横屏左旋 90°</p></Tooltip.Content>
			</Tooltip.Root>

			<Tooltip.Root>
				<Tooltip.Trigger>
					<Button variant={autoRotateMode === 'horizontalRight' ? 'default' : 'ghost'} size="icon" className="h-7 w-7 rounded-full" onClick={() => setAutoRotateMode('horizontalRight')}>
						<Smartphone className="h-3.5 w-3.5 rotate-90"></Smartphone>
					</Button>
				</Tooltip.Trigger>
				<Tooltip.Content><p>横屏右旋 90°</p></Tooltip.Content>
			</Tooltip.Root>
		</div>

		<Separator.Root orientation="vertical" className="mx-2 h-5"></Separator.Root>

		<span className="text-muted-foreground mr-2 text-xs">强制</span>
		<div className="bg-muted/60 inline-flex items-center gap-0.5 rounded-full p-0.5 shadow-inner">
			<Tooltip.Root>
				<Tooltip.Trigger>
					<Button variant={autoRotateMode === 'forcedLeft' ? 'default' : 'ghost'} size="icon" className="h-7 w-7 rounded-full" onClick={() => setAutoRotateMode('forcedLeft')}>
						<RotateCcw className="h-3.5 w-3.5"></RotateCcw>
					</Button>
				</Tooltip.Trigger>
				<Tooltip.Content><p>始终左旋 90°</p></Tooltip.Content>
			</Tooltip.Root>

			<Tooltip.Root>
				<Tooltip.Trigger>
					<Button variant={autoRotateMode === 'forcedRight' ? 'default' : 'ghost'} size="icon" className="h-7 w-7 rounded-full" onClick={() => setAutoRotateMode('forcedRight')}>
						<RotateCw className="h-3.5 w-3.5"></RotateCw>
					</Button>
				</Tooltip.Trigger>
				<Tooltip.Content><p>始终右旋 90°</p></Tooltip.Content>
			</Tooltip.Root>
		</div>
	</div>
</Fragment>) : null}</Fragment>
  )
}
