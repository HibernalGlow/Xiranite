import { useEffect, useRef, useState } from "react"
import type { DragEvent } from "react"
import type { CzkawkaSelectionApplyMode, CzkawkaSelectionAssistantConfig, CzkawkaSelectionMatchCondition, CzkawkaSelectionResult, CzkawkaSelectionSortCriterion, CzkawkaSelectionSortField, CzkawkaSelectionStats, CzkawkaSelectionTextColumn } from "@xiranite/node-czkawka/selection-assistant"
import { createDefaultCzkawkaSelectionAssistantConfig, parseCzkawkaSelectionAssistantConfig, serializeCzkawkaSelectionAssistantConfig } from "@xiranite/node-czkawka/selection-assistant"
import { Check, ChevronDown, ChevronUp, GripVertical, Plus, Redo2, RotateCcw, Save, Sparkles, ToggleLeft, Trash2, Undo2, Upload } from "lucide-react"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover"
import { ScrollArea } from "@/components/ui/scroll-area"
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
import { Switch } from "@/components/ui/switch"
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
import { Textarea } from "@/components/ui/textarea"

type RuleKind = "group" | "text" | "directory"

export interface CzkawkaSelectionAssistantProps {
  open: boolean
  config: CzkawkaSelectionAssistantConfig
  stats: CzkawkaSelectionStats
  canUndo: boolean
  canRedo: boolean
  onOpenChange: (open: boolean) => void
  onConfigChange: (config: CzkawkaSelectionAssistantConfig) => void
  onApply: (kind: RuleKind) => CzkawkaSelectionResult
  onUndo: () => void
  onRedo: () => void
  onClear: () => void
  onInvert: () => void
  onSelectAll: () => void
}

export function CzkawkaSelectionAssistant(props: CzkawkaSelectionAssistantProps) {
  "use no memo"
  const triggerRef = useRef<HTMLButtonElement>(null)
  const [tab, setTab] = useState<RuleKind>("group")
  const [dragIndex, setDragIndex] = useState<number | null>(null)
  const [message, setMessage] = useState("")
  const [transferText, setTransferText] = useState("")

  useEffect(() => {
    const handleKeyDown = (event: KeyboardEvent) => {
      const surface = triggerRef.current?.closest('[data-testid="czkawka-surface"]')
      if (!props.open && surface && !surface.contains(document.activeElement)) return
      const target = event.target as HTMLElement | null
      const typing = target?.tagName === "INPUT" || target?.tagName === "TEXTAREA" || target?.isContentEditable
      if ((event.ctrlKey || event.metaKey) && event.key.toLocaleLowerCase() === "z" && !event.shiftKey && !typing) { event.preventDefault(); props.onUndo(); return }
      if ((event.ctrlKey || event.metaKey) && (event.key.toLocaleLowerCase() === "y" || event.shiftKey && event.key.toLocaleLowerCase() === "z") && !typing) { event.preventDefault(); props.onRedo(); return }
      if ((event.ctrlKey || event.metaKey) && event.key === "Enter" && !typing) { event.preventDefault(); apply(tab); return }
      if ((event.ctrlKey || event.metaKey) && event.key === "Backspace" && !typing) { event.preventDefault(); props.onClear() }
    }
    window.addEventListener("keydown", handleKeyDown)
    return () => window.removeEventListener("keydown", handleKeyDown)
  }, [props.open, props.onClear, props.onRedo, props.onUndo, tab])

  function patch<K extends keyof CzkawkaSelectionAssistantConfig>(key: K, value: CzkawkaSelectionAssistantConfig[K]) { props.onConfigChange({ ...props.config, [key]: value }) }
  function apply(kind: RuleKind) { const result = props.onApply(kind); setMessage(result.error ?? `已匹配 ${result.matchedPaths.length} 项，改变 ${result.affectedCount} 项。`) }
  function reorderCriterion(from: number, to: number) { if (to < 0 || to >= props.config.group.sortCriteria.length || from === to) return; const next = [...props.config.group.sortCriteria]; const [item] = next.splice(from, 1); next.splice(to, 0, item!); patch("group", { ...props.config.group, sortCriteria: next }) }
  function updateCriterion(index: number, value: Partial<CzkawkaSelectionSortCriterion>) { patch("group", { ...props.config.group, sortCriteria: props.config.group.sortCriteria.map((item, itemIndex) => itemIndex === index ? { ...item, ...value } : item) }) }
  function dropCriterion(event: DragEvent, index: number) { event.preventDefault(); if (dragIndex !== null) reorderCriterion(dragIndex, index); setDragIndex(null) }
  function importConfig() { try { props.onConfigChange(parseCzkawkaSelectionAssistantConfig(transferText)); setMessage("配置导入成功。") } catch (error) { setMessage(error instanceof Error ? error.message : String(error)) } }

  return <Popover open={props.open} onOpenChange={props.onOpenChange}><PopoverTrigger asChild><Button ref={triggerRef} size="sm" variant="outline"><Sparkles />选择助手<Badge variant="secondary">{props.stats.selectedCount}</Badge></Button></PopoverTrigger><PopoverContent align="end" className="w-[min(96vw,640px)] p-0"><div className="flex items-center justify-between border-b px-3 py-2"><div><div className="text-sm font-semibold">智能选择助手</div><div className="text-[11px] text-muted-foreground">{props.stats.selectedCount} 项 · {formatBytes(props.stats.selectedBytes)} · 可回收 {formatBytes(props.stats.reclaimableBytes)}</div></div><div className="flex gap-1"><Button aria-label="撤销选择" disabled={!props.canUndo} size="icon-sm" variant="ghost" onClick={props.onUndo}><Undo2 /></Button><Button aria-label="重做选择" disabled={!props.canRedo} size="icon-sm" variant="ghost" onClick={props.onRedo}><Redo2 /></Button></div></div><ScrollArea className="h-[min(72vh,680px)]"><div className="grid gap-3 p-3"><section className="grid gap-2 rounded-md border p-2"><div className="text-xs font-semibold">应用模式</div><Select value={props.config.applyMode} onValueChange={(applyMode) => patch("applyMode", applyMode as CzkawkaSelectionApplyMode)}><SelectTrigger aria-label="选择应用模式"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="replace">替换选择</SelectItem><SelectItem value="add">添加到选择</SelectItem><SelectItem value="remove">从选择移除</SelectItem><SelectItem value="intersect">与当前选择取交集</SelectItem></SelectContent></Select></section><Tabs value={tab} onValueChange={(value) => setTab(value as RuleKind)}><TabsList className="grid grid-cols-3"><TabsTrigger value="group">组规则</TabsTrigger><TabsTrigger value="text">文本规则</TabsTrigger><TabsTrigger value="directory">目录规则</TabsTrigger></TabsList><TabsContent value="group" className="grid gap-3"><GroupRuleEditor config={props.config} dragIndex={dragIndex} setDragIndex={setDragIndex} patch={patch} updateCriterion={updateCriterion} reorderCriterion={reorderCriterion} dropCriterion={dropCriterion} /><Button onClick={() => apply("group")}><Check />应用组规则</Button></TabsContent><TabsContent value="text" className="grid gap-3"><TextRuleEditor config={props.config} patch={patch} /><Button disabled={!props.config.text.pattern} onClick={() => apply("text")}><Check />应用文本规则</Button></TabsContent><TabsContent value="directory" className="grid gap-3"><DirectoryRuleEditor config={props.config} patch={patch} /><Button onClick={() => apply("directory")}><Check />应用目录规则</Button></TabsContent></Tabs><section className="grid gap-2 rounded-md border p-2"><div className="grid grid-cols-3 gap-2"><Button size="sm" variant="outline" onClick={props.onSelectAll}><Check />全选可见项</Button><Button size="sm" variant="outline" onClick={props.onInvert}><ToggleLeft />反选可见项</Button><Button size="sm" variant="outline" onClick={props.onClear}><RotateCcw />清空选择</Button></div><div className="flex gap-2"><Button size="xs" variant="ghost" onClick={() => setTransferText(serializeCzkawkaSelectionAssistantConfig(props.config))}><Save />导出配置</Button><Button size="xs" variant="ghost" onClick={importConfig}><Upload />导入配置</Button><Button size="xs" variant="ghost" onClick={() => props.onConfigChange(createDefaultCzkawkaSelectionAssistantConfig())}>重置规则</Button></div>{transferText ? <Textarea aria-label="选择助手配置 JSON" className="min-h-24 font-mono text-[10px]" value={transferText} onChange={(event) => setTransferText(event.currentTarget.value)} /> : null}</section>{message ? <div role={message.includes("成功") || message.startsWith("已匹配") ? "status" : "alert"} className="rounded-md border px-2 py-1.5 text-xs">{message}</div> : null}<div className="text-[10px] text-muted-foreground">快捷键：Ctrl/Cmd+Enter 应用当前规则 · Ctrl/Cmd+Z 撤销 · Ctrl/Cmd+Y 重做 · Ctrl/Cmd+Backspace 清空</div></div></ScrollArea></PopoverContent></Popover>
}

function GroupRuleEditor({ config, dragIndex, setDragIndex, patch, updateCriterion, reorderCriterion, dropCriterion }: { config: CzkawkaSelectionAssistantConfig; dragIndex: number | null; setDragIndex: (index: number | null) => void; patch: <K extends keyof CzkawkaSelectionAssistantConfig>(key: K, value: CzkawkaSelectionAssistantConfig[K]) => void; updateCriterion: (index: number, value: Partial<CzkawkaSelectionSortCriterion>) => void; reorderCriterion: (from: number, to: number) => void; dropCriterion: (event: DragEvent, index: number) => void }) {
  function addCriterion() { const index = config.group.sortCriteria.length; patch("group", { ...config.group, sortCriteria: [...config.group.sortCriteria, { id: `criterion-${Date.now()}-${index}`, field: "fileSize", direction: "desc", preferEmpty: false, enabled: true, filterCondition: "none", filterValue: "" }] }) }
  return <><Select value={config.group.mode} onValueChange={(mode) => patch("group", { ...config.group, mode: mode as typeof config.group.mode })}><SelectTrigger aria-label="组选择模式"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all-except-one">每组除一个外全选</SelectItem><SelectItem value="select-one">每组只选一个</SelectItem><SelectItem value="all-except-one-per-folder">每目录除一个外全选</SelectItem><SelectItem value="all-except-one-matching-set">除一个匹配集外全选</SelectItem></SelectContent></Select><div className="grid gap-2">{config.group.sortCriteria.map((criterion, index) => <div key={criterion.id} draggable onDragStart={() => setDragIndex(index)} onDragEnd={() => setDragIndex(null)} onDragOver={(event) => event.preventDefault()} onDrop={(event) => dropCriterion(event, index)} data-dragging={dragIndex === index || undefined} className="grid gap-2 rounded-md border p-2 data-[dragging=true]:opacity-50"><div className="flex items-center gap-1"><GripVertical className="size-4 cursor-grab text-muted-foreground" /><Switch aria-label={`启用排序条件 ${index + 1}`} checked={criterion.enabled} size="sm" onCheckedChange={(enabled) => updateCriterion(index, { enabled })} /><Select value={criterion.field} onValueChange={(field) => updateCriterion(index, { field: field as CzkawkaSelectionSortField })}><SelectTrigger aria-label={`排序字段 ${index + 1}`} className="flex-1"><SelectValue /></SelectTrigger><SelectContent>{SORT_FIELDS.map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select><Select value={criterion.direction} onValueChange={(direction) => updateCriterion(index, { direction: direction as "asc" | "desc" })}><SelectTrigger aria-label={`排序方向 ${index + 1}`} className="w-24"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="asc">升序</SelectItem><SelectItem value="desc">降序</SelectItem></SelectContent></Select><Button aria-label={`上移排序条件 ${index + 1}`} disabled={index === 0} size="icon-sm" variant="ghost" onClick={() => reorderCriterion(index, index - 1)}><ChevronUp /></Button><Button aria-label={`下移排序条件 ${index + 1}`} disabled={index === config.group.sortCriteria.length - 1} size="icon-sm" variant="ghost" onClick={() => reorderCriterion(index, index + 1)}><ChevronDown /></Button><Button aria-label={`删除排序条件 ${index + 1}`} size="icon-sm" variant="ghost" onClick={() => patch("group", { ...config.group, sortCriteria: config.group.sortCriteria.filter((_, itemIndex) => itemIndex !== index) })}><Trash2 /></Button></div><div className="grid grid-cols-[130px_1fr_auto] gap-2"><Select value={criterion.filterCondition} onValueChange={(filterCondition) => updateCriterion(index, { filterCondition: filterCondition as CzkawkaSelectionMatchCondition })}><SelectTrigger aria-label={`排序过滤条件 ${index + 1}`}><SelectValue /></SelectTrigger><SelectContent>{MATCH_CONDITIONS.map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select><Input aria-label={`排序过滤值 ${index + 1}`} disabled={criterion.filterCondition === "none"} value={criterion.filterValue} onChange={(event) => updateCriterion(index, { filterValue: event.currentTarget.value })} /><label className="flex items-center gap-1 text-xs"><Switch aria-label={`空值优先 ${index + 1}`} checked={criterion.preferEmpty} size="sm" onCheckedChange={(preferEmpty) => updateCriterion(index, { preferEmpty })} />空值优先</label></div></div>)}</div><Button size="sm" variant="outline" onClick={addCriterion}><Plus />添加排序条件</Button></>
}

function TextRuleEditor({ config, patch }: EditorProps) { return <><Select value={config.text.column} onValueChange={(column) => patch("text", { ...config.text, column: column as CzkawkaSelectionTextColumn })}><SelectTrigger aria-label="文本规则字段"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="fullPath">完整路径</SelectItem><SelectItem value="fileName">文件名</SelectItem><SelectItem value="folderPath">文件夹路径</SelectItem></SelectContent></Select><Select value={config.text.condition} onValueChange={(condition) => patch("text", { ...config.text, condition: condition as typeof config.text.condition })}><SelectTrigger aria-label="文本匹配条件"><SelectValue /></SelectTrigger><SelectContent>{MATCH_CONDITIONS.filter(([value]) => value !== "none").map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select><Input aria-label="文本规则模式" value={config.text.pattern} onChange={(event) => patch("text", { ...config.text, pattern: event.currentTarget.value })} /><div className="grid grid-cols-3 gap-2"><Toggle label="正则表达式" value={config.text.useRegex} onChange={(useRegex) => patch("text", { ...config.text, useRegex })} /><Toggle label="区分大小写" value={config.text.caseSensitive} onChange={(caseSensitive) => patch("text", { ...config.text, caseSensitive })} /><Toggle label="整列匹配" value={config.text.matchWholeColumn} onChange={(matchWholeColumn) => patch("text", { ...config.text, matchWholeColumn })} /></div></> }
function DirectoryRuleEditor({ config, patch }: EditorProps) { return <><Select value={config.directory.mode} onValueChange={(mode) => patch("directory", { ...config.directory, mode: mode as typeof config.directory.mode })}><SelectTrigger aria-label="目录规则模式"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="keep-one-per-directory">每目录保留一个</SelectItem><SelectItem value="select-all-in-directory">选择目录内全部</SelectItem><SelectItem value="exclude-directory">排除目录中的选择</SelectItem></SelectContent></Select><Textarea aria-label="选择规则目录" className="min-h-24 font-mono text-xs" placeholder="每行一个目录" value={config.directory.directories.join("\n")} onChange={(event) => patch("directory", { ...config.directory, directories: lines(event.currentTarget.value) })} /><div className="text-[10px] text-muted-foreground">参考目录中的参考项始终受保护，不会进入可操作选择。</div></> }
function Toggle({ label, value, onChange }: { label: string; value: boolean; onChange: (value: boolean) => void }) { return <label className="flex items-center justify-between gap-1 rounded-md border px-2 py-1 text-xs"><span>{label}</span><Switch aria-label={label} checked={value} size="sm" onCheckedChange={onChange} /></label> }
type EditorProps = { config: CzkawkaSelectionAssistantConfig; patch: <K extends keyof CzkawkaSelectionAssistantConfig>(key: K, value: CzkawkaSelectionAssistantConfig[K]) => void }
const SORT_FIELDS: Array<[CzkawkaSelectionSortField, string]> = [["folderPath", "文件夹"], ["fileName", "文件名"], ["fileSize", "大小"], ["creationDate", "创建时间"], ["modifiedDate", "修改时间"], ["resolution", "分辨率"], ["disk", "磁盘"], ["fileType", "扩展名"], ["hash", "哈希"], ["hardLinks", "硬链接"]]
const MATCH_CONDITIONS: Array<[CzkawkaSelectionMatchCondition, string]> = [["none", "无过滤"], ["contains", "包含"], ["not-contains", "不包含"], ["starts-with", "开头是"], ["ends-with", "结尾是"], ["equals", "等于"]]
function lines(value: string): string[] { return [...new Set(value.split(/\r?\n/).map((item) => item.trim()).filter(Boolean))] }
function formatBytes(bytes: number): string { if (bytes < 1024) return `${bytes} B`; const units = ["KB", "MB", "GB", "TB"]; let value = bytes / 1024, unit = units[0]!; for (let index = 1; index < units.length && value >= 1024; index += 1) { value /= 1024; unit = units[index]! } return `${value.toFixed(value >= 10 ? 1 : 2)} ${unit}` }
