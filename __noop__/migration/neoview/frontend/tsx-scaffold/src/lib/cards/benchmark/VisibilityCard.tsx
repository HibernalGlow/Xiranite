/**
 * @migrated-from src/lib/cards/benchmark/VisibilityCard.svelte
 * @source-hash sha256:6e2099dc59c94f296d11be9380434bcf89234ba88dd02349aeeb1f46479867e4
 * @features ai-ollama-translation,card-windows-tabs,file-browser-navigation,history-bookmarks-progress,input-bindings-radial-voice,metadata-dimensions-properties,page-index-navigation,page-layout-modes,panels-toolbar-shell,performance-benchmark-diagnostics,playlist-quick-library,search-sort-filter-library,settings-import-export-backup,startup-window-cli-lifecycle,super-resolution,theme-background-empty-state
 * @source-disposition manual
 * @migration-status review-scaffold
 * Generated by @xiranite/svelte-migrate; move to production only after behavior tests pass.
 */
import { Fragment } from "react"
import { Button } from '$lib/components/ui/button';
import { visibilityMonitor, setMonitorEnabled } from '$lib/stores/visibilityMonitor.js';

export function VisibilityCard() {
  return (
    <Fragment>

<div className="space-y-2">
	{/*  开关按钮  */}
	<div className="flex items-center justify-between">
		<p className="text-muted-foreground text-[10px]">实时监控 VirtualizedFileListV2 的可见条目范围</p>
		<Button variant={visibilityMonitor.enabled ? 'default' : 'outline'} size="sm" className="h-6 px-2 text-[10px]" onClick={() => setMonitorEnabled(!visibilityMonitor.enabled)}>
			{visibilityMonitor.enabled ? '关闭监控' : '开启监控'}
		</Button>
	</div>

	{!visibilityMonitor.enabled ? (<Fragment>
		<div className="text-muted-foreground bg-muted/20 rounded border py-4 text-center text-[10px]">
			⏸️ 监控已关闭，点击上方按钮开启
		</div>
	</Fragment>) : (<Fragment>{visibilityMonitor.info.totalItems > 0 ? (<Fragment>
		<div className="space-y-2 rounded border p-2 text-[10px]">
			{/*  当前路径  */}
			<div className="text-muted-foreground truncate" title={visibilityMonitor.info.currentPath}>
				📁 {visibilityMonitor.info.currentPath.split(/[/\\]/).pop() || '根目录'}
			</div>

			{/*  可见范围  */}
			<div className="grid grid-cols-3 gap-2">
				<div className="bg-muted/30 rounded p-1 text-center">
					<div className="font-mono">{visibilityMonitor.info.visibleStart}</div>
					<div className="text-muted-foreground">起始</div>
				</div>
				<div className="bg-primary/10 rounded p-1 text-center">
					<div className="font-mono">{visibilityMonitor.info.visibleCount}</div>
					<div className="text-muted-foreground">可见</div>
				</div>
				<div className="bg-muted/30 rounded p-1 text-center">
					<div className="font-mono">{visibilityMonitor.info.totalItems}</div>
					<div className="text-muted-foreground">总数</div>
				</div>
			</div>

			{/*  进度条  */}
			<div className="bg-muted h-2 overflow-hidden rounded-full">
				<div className="bg-primary h-full transition-all duration-150" style={`margin-left: ${(visibilityMonitor.info.visibleStart /
						visibilityMonitor.info.totalItems) *
						100}%; width: ${(visibilityMonitor.info.visibleCount /
						visibilityMonitor.info.totalItems) *
						100}%`}></div>
			</div>
		</div>
	</Fragment>) : (<Fragment>
		<div className="text-muted-foreground bg-muted/20 rounded border py-4 text-center text-[10px]">
			📂 等待文件列表加载...
		</div>
	</Fragment>)}</Fragment>)}
</div></Fragment>
  )
}
