import type { InteractionField,InteractionValues,TerminalInteractionSchema } from "@xiranite/cli-runtime/interaction"
import type { TerminalLanguage } from "@xiranite/cli-runtime/i18n"
import type { CoveruAction,CoveruInput,CoveruResult } from "./core.js"
export type CoveruInteractionValues=InteractionValues&{action:CoveruAction;paths:string;outputDir:string;outputMode:"alongside"|"directory";overwrite:boolean;recursive:boolean;dryRun:boolean;preferred:string}
export function createCoveruInteractionSchema(d:Partial<CoveruInteractionValues>={},lang:TerminalLanguage="zh"):TerminalInteractionSchema<CoveruInput,CoveruResult>{const zh=lang==="zh",initialValues:CoveruInteractionValues={action:"scan",paths:"",outputDir:"",outputMode:"alongside",overwrite:false,recursive:true,dryRun:true,preferred:"cover, folder, front, 000, 001",...d};const extract=(v:Readonly<InteractionValues>)=>v.action==="extract";const fields:InteractionField[]=[{id:"action",label:zh?"封面操作":"Cover action",kind:"select",role:"action",options:[{value:"scan",label:zh?"⌕ 扫描":"Scan"},{value:"plan",label:zh?"◌ 预演":"Plan"},{value:"extract",label:zh?"▣ 提取":"Extract"}]},{id:"paths",label:zh?"输入队列":"Input queue",kind:"path-list",lines:5},{id:"outputMode",label:zh?"输出位置":"Output location",kind:"select",options:[{value:"alongside",label:zh?"同目录":"Alongside"},{value:"directory",label:zh?"指定目录":"Directory"}]},{id:"outputDir",label:zh?"输出目录":"Output directory",kind:"text",visibleWhen:v=>v.outputMode==="directory"},{id:"preferred",label:zh?"优先名称":"Preferred names",kind:"text"},{id:"recursive",label:zh?"递归扫描":"Recursive",kind:"boolean"},{id:"overwrite",label:zh?"覆盖已有文件":"Overwrite",kind:"boolean",visibleWhen:extract},{id:"dryRun",label:zh?"仅预演":"Dry run",kind:"boolean",visibleWhen:extract}];return {id:"coveru",title:"CoverU",description:zh?"压缩包封面评分、冲突检查与安全提取":"Cover scoring and safe extraction",initialValues,fields,view:{sections:[{id:"covers",title:zh?"封面提取":"Cover extraction",fieldIds:fields.map(f=>f.id)}],dashboard:{title:"CoverU",display:v=>({primary:String(v.action),secondary:v.dryRun!==false?"Preview":"Live",metrics:[]})}},toInput:v=>({action:v.action==="plan"||v.action==="extract"?v.action:"scan",paths:String(v.paths??"").split(/[\r\n;]+/).map(x=>x.trim()).filter(Boolean),outputDir:String(v.outputDir??""),outputMode:v.outputMode==="directory"?"directory":"alongside",overwrite:v.overwrite===true,recursive:v.recursive!==false,dryRun:v.action!=="extract"||v.dryRun!==false,preferredNames:String(v.preferred??"").split(",").map(x=>x.trim()).filter(Boolean)}),validate(_v,i){return i.paths?.length?null:zh?"至少输入一个归档或目录。":"Enter at least one archive or directory."},preview:i=>[`${zh?"输入":"Inputs"}: ${i.paths?.length??0}`,i.dryRun!==false?zh?"只生成候选计划":"Plan only":zh?"将写入封面文件":"Will write cover files"],isDangerous:i=>i.action==="extract"&&i.dryRun===false,dangerPrompt:()=>({title:zh?"确认提取封面":"Confirm cover extraction",body:zh?"将把评分最高的封面提取到目标位置。":"Highest-scored covers will be extracted to their targets.",confirmLabel:zh?"确认提取":"Extract"}),result:r=>({success:r.success,message:r.message,lines:r.data?[`Ready: ${r.data.readyCount}`,`Extracted: ${r.data.extractedCount}`,`Errors: ${r.data.errorCount}`]:[]})}}
