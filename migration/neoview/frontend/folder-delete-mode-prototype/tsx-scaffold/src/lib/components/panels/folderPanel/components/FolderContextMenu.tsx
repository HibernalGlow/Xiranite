/**
 * @migrated-from src/lib/components/panels/folderPanel/components/FolderContextMenu.svelte
 * @source-hash sha256:e52028cac1c0685dee8625dddea913013e1bf03a5cbf369b7357637d342e38f5
 * @features
 * @source-disposition manual
 * @migration-status partial-scaffold
 * @unsupported attribute UseDirective; template node SnippetBlock
 * Generated by @xiranite/svelte-migrate; move to production only after behavior tests pass.
 */
import { Fragment } from "react"
import { mergeProps } from 'bits-ui';
import * as Tooltip from '$lib/components/ui/tooltip/index.js';
import type { FsItem } from '$lib/types';
import {
		Folder,
		File,
		BookOpen,
		ExternalLink,
		Copy,
		Scissors,
		ClipboardPaste,
		Trash2,
		Star,
		Pencil,
		Play,
		FolderOpen,
		PanelRight,
		RefreshCw,
		Tags,
		Undo2
	} from 'lucide-react';
import { Pin } from 'lucide-react';
import { folderTreePinStore } from '$lib/stores/folderTreePin.js';
interface Props {
		item: FsItem | null;
		x: number;
		y: number;
		visible: boolean;
		onClose: () => void;
		onOpenAsBook?: (item: FsItem) => void;
		onBrowse?: (item: FsItem) => void;
		onOpenInNewTab?: (item: FsItem) => void;
		onCopy?: (item: FsItem) => void;
		onCut?: (item: FsItem) => void;
		onPaste?: () => void;
		onDelete?: (item: FsItem) => void;
		onRename?: (item: FsItem) => void;
		onAddBookmark?: (item: FsItem) => void;
		onToggleFolderTreePin?: (item: FsItem) => void;
		onCopyPath?: (item: FsItem) => void;
		onCopyName?: (item: FsItem) => void;
		onOpenInExplorer?: (item: FsItem) => void;
		onOpenWithSystem?: (item: FsItem) => void;
		onReloadThumbnail?: (item: FsItem) => void;
		onEditTags?: (item: FsItem) => void;
		onUndoDelete?: () => void;
	}

export function FolderContextMenu(props: Props) {
  const {
		item,
		x,
		y,
		visible,
		onClose,
		onOpenAsBook,
		onBrowse,
		onOpenInNewTab,
		onCopy,
		onCut,
		onPaste,
		onDelete,
		onRename,
		onAddBookmark,
		onToggleFolderTreePin,
		onCopyPath,
		onCopyName,
		onOpenInExplorer,
		onOpenWithSystem,
		onReloadThumbnail,
		onEditTags,
		onUndoDelete
	} = props
  let isPinnedInFolderTree = $state(false);

  $effect(() => {
      if (!visible || !item) {
        isPinnedInFolderTree = false;
        return;
      }
      const unsubscribe = folderTreePinStore.subscribe((paths) => {
        const key = item.path.replace(/\\/g, '/').toLowerCase();
        isPinnedInFolderTree = paths.includes(key);
      });
      return unsubscribe;
    });

  let menuX = $state(0);

  let menuY = $state(0);

  let maxHeight = $state(400);

  function portal(node: HTMLElement) {
      // 将元素移动到 body
      document.body.appendChild(node);

      return {
        destroy() {
          // 组件销毁时移除元素
          if (node.parentNode) {
            node.parentNode.removeChild(node);
          }
        }
      };
    }

  $effect(() => {
      if (visible && item) {
        const viewportWidth = window.innerWidth;
        const viewportHeight = window.innerHeight;
        const menuWidth = 220; // 菜单估计宽度
        const padding = 8; // 距离边界的最小间距

        // 计算水平位置
        let finalX = x;
        if (x + menuWidth > viewportWidth - padding) {
          // 超出右边界，向左调整
          finalX = Math.max(padding, viewportWidth - menuWidth - padding);
        }

        // 总是向下显示，计算下方可用空间作为最大高度
        // 如果下方空间太小，允许溢出屏幕或通过内部滚动处理
        maxHeight = Math.max(100, viewportHeight - y - padding);

        menuX = finalX;
        menuY = y;
      }
    });

  function handleOpenAsBook() {
      if (item) onOpenAsBook?.(item);
      onClose();
    }

  function handleBrowse() {
      if (item) onBrowse?.(item);
      onClose();
    }

  function handleOpenInNewTab() {
      if (item) onOpenInNewTab?.(item);
      onClose();
    }

  function handleCopy() {
      if (item) onCopy?.(item);
      onClose();
    }

  function handleCut() {
      if (item) onCut?.(item);
      onClose();
    }

  function handlePaste() {
      onPaste?.();
      onClose();
    }

  function handleDelete() {
      if (item) onDelete?.(item);
      onClose();
    }

  function handleRename() {
      if (item) onRename?.(item);
      onClose();
    }

  function handleAddBookmark() {
      if (item) onAddBookmark?.(item);
      onClose();
    }

  function handleToggleFolderTreePin() {
      if (item) onToggleFolderTreePin?.(item);
      onClose();
    }

  function handleCopyPath() {
      if (item) onCopyPath?.(item);
      onClose();
    }

  function handleCopyName() {
      if (item) onCopyName?.(item);
      onClose();
    }

  function handleOpenInExplorer() {
      if (item) onOpenInExplorer?.(item);
      onClose();
    }

  function handleOpenWithSystem() {
      if (item) onOpenWithSystem?.(item);
      onClose();
    }

  function handleReloadThumbnail() {
      if (item) onReloadThumbnail?.(item);
      onClose();
    }

  function handleEditTags() {
      if (item) onEditTags?.(item);
      onClose();
    }

  function handleUndoDelete() {
      onUndoDelete?.();
      onClose();
    }
  return (
    <Fragment>

{visible && item ? (<Fragment>
	{/*  svelte-ignore a11y_click_events_have_key_events  */}
	{/*  svelte-ignore a11y_no_static_element_interactions  */}
	<div className="context-menu pointer-events-auto fixed" style={`left: ${menuX}px; top: ${menuY}px; z-index: 9999;`} onClick={(e) => e.stopPropagation()} onMouseDown={(e) => e.stopPropagation()}>
		<div className="bg-popover/80 text-popover-foreground min-w-45 overflow-hidden rounded-md border shadow-md backdrop-blur-md" style={`max-height: ${maxHeight}px;`}>
			<div className="overflow-y-auto p-1" style={`max-height: ${maxHeight}px;`}>
				{/*  Row 1: Common Actions  */}
				<div className="flex flex-row items-center justify-between gap-1 p-1">
					<Tooltip.Root delayDuration={400}>
						<Tooltip.Trigger>
							{/* TODO unsupported SnippetBlock */}
						</Tooltip.Trigger>
						<Tooltip.Content side="bottom">剪切</Tooltip.Content>
					</Tooltip.Root>

					<Tooltip.Root delayDuration={400}>
						<Tooltip.Trigger>
							{/* TODO unsupported SnippetBlock */}
						</Tooltip.Trigger>
						<Tooltip.Content side="bottom">复制</Tooltip.Content>
					</Tooltip.Root>

					<Tooltip.Root delayDuration={400}>
						<Tooltip.Trigger>
							{/* TODO unsupported SnippetBlock */}
						</Tooltip.Trigger>
						<Tooltip.Content side="bottom">粘贴</Tooltip.Content>
					</Tooltip.Root>

					<Tooltip.Root delayDuration={400}>
						<Tooltip.Trigger>
							{/* TODO unsupported SnippetBlock */}
						</Tooltip.Trigger>
						<Tooltip.Content side="bottom">删除</Tooltip.Content>
					</Tooltip.Root>

					<Tooltip.Root delayDuration={400}>
						<Tooltip.Trigger>
							{/* TODO unsupported SnippetBlock */}
						</Tooltip.Trigger>
						<Tooltip.Content side="bottom">重命名</Tooltip.Content>
					</Tooltip.Root>
				</div>

				{/*  Row 2: Navigation Actions  */}
				<div className="flex flex-row items-center justify-start gap-1 p-1">
					{item.isDir ? (<Fragment>
						<Tooltip.Root delayDuration={400}>
							<Tooltip.Trigger>
								{/* TODO unsupported SnippetBlock */}
							</Tooltip.Trigger>
							<Tooltip.Content side="bottom">浏览</Tooltip.Content>
						</Tooltip.Root>

						<Tooltip.Root delayDuration={400}>
							<Tooltip.Trigger>
								{/* TODO unsupported SnippetBlock */}
							</Tooltip.Trigger>
							<Tooltip.Content side="bottom">在新标签页打开</Tooltip.Content>
						</Tooltip.Root>

						<Tooltip.Root delayDuration={400}>
							<Tooltip.Trigger>
								{/* TODO unsupported SnippetBlock */}
							</Tooltip.Trigger>
							<Tooltip.Content side="bottom">作为书籍打开</Tooltip.Content>
						</Tooltip.Root>
					</Fragment>) : (<Fragment>
						<Tooltip.Root delayDuration={400}>
							<Tooltip.Trigger>
								{/* TODO unsupported SnippetBlock */}
							</Tooltip.Trigger>
							<Tooltip.Content side="bottom">打开</Tooltip.Content>
						</Tooltip.Root>

						<Tooltip.Root delayDuration={400}>
							<Tooltip.Trigger>
								{/* TODO unsupported SnippetBlock */}
							</Tooltip.Trigger>
							<Tooltip.Content side="bottom">打开所在文件夹</Tooltip.Content>
						</Tooltip.Root>

						<Tooltip.Root delayDuration={400}>
							<Tooltip.Trigger>
								{/* TODO unsupported SnippetBlock */}
							</Tooltip.Trigger>
							<Tooltip.Content side="bottom">用默认软件打开</Tooltip.Content>
						</Tooltip.Root>
					</Fragment>)}

					<Tooltip.Root delayDuration={400}>
						<Tooltip.Trigger>
							{/* TODO unsupported SnippetBlock */}
						</Tooltip.Trigger>
						<Tooltip.Content side="bottom">在资源管理器中打开</Tooltip.Content>
					</Tooltip.Root>

					<Tooltip.Root delayDuration={400}>
						<Tooltip.Trigger>
							{/* TODO unsupported SnippetBlock */}
						</Tooltip.Trigger>
						<Tooltip.Content side="bottom">撤回上一次删除</Tooltip.Content>
					</Tooltip.Root>
				</div>

				<div className="bg-border my-1 h-px"></div>

				{/*  其他操作  */}
				<button className="hover:bg-accent hover:text-accent-foreground flex w-full items-center gap-2 rounded-sm px-2 py-1.5 text-sm" onClick={handleAddBookmark}>
					<Star className="h-4 w-4"></Star>
					<span>添加书签</span>
				</button>
				<button className="hover:bg-accent hover:text-accent-foreground flex w-full items-center gap-2 rounded-sm px-2 py-1.5 text-sm" onClick={handleToggleFolderTreePin}>
					<Pin className="h-4 w-4"></Pin>
					<span>{isPinnedInFolderTree ? '取消文件树置顶' : '在文件树中置顶'}</span>
				</button>
				<button className="hover:bg-accent hover:text-accent-foreground flex w-full items-center gap-2 rounded-sm px-2 py-1.5 text-sm" onClick={handleEditTags}>
					<Tags className="h-4 w-4"></Tags>
					<span>编辑标签</span>
				</button>

				<div className="bg-border my-1 h-px"></div>

				<button className="hover:bg-accent hover:text-accent-foreground flex w-full items-center gap-2 rounded-sm px-2 py-1.5 text-sm" onClick={handleCopyPath}>
					<Copy className="h-4 w-4"></Copy>
					<span>复制路径</span>
				</button>
				<button className="hover:bg-accent hover:text-accent-foreground flex w-full items-center gap-2 rounded-sm px-2 py-1.5 text-sm" onClick={handleCopyName}>
					<File className="h-4 w-4"></File>
					<span>复制文件名</span>
				</button>

				<div className="bg-border my-1 h-px"></div>

				<button className="hover:bg-accent hover:text-accent-foreground flex w-full items-center gap-2 rounded-sm px-2 py-1.5 text-sm" onClick={handleReloadThumbnail}>
					<RefreshCw className="h-4 w-4"></RefreshCw>
					<span>重载缩略图</span>
				</button>
			</div>
		</div>
	</div>
</Fragment>) : null}

{/*  点击外部关闭菜单  */}
{visible ? (<Fragment>
	{/*  svelte-ignore a11y_click_events_have_key_events  */}
	{/*  svelte-ignore a11y_no_static_element_interactions  */}
	<div className="fixed inset-0" style="z-index: 9998;" onClick={onClose} onMouseDown={(e) => e.stopPropagation()}></div>
</Fragment>) : null}</Fragment>
  )
}
