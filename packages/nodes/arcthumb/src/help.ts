import type { NodeHelp } from "@xiranite/contract"
export const help = { title: "ArcThumb", short: "Generate native archive and ebook cover thumbnails.", description: "Uses the ArcThumb Rust core for comic archives and ebooks without decoding image bytes in JavaScript.", whenToUse: ["Use ArcThumb to inspect or write representative covers for CBZ, CBR, CB7, CBT, EPUB, FB2, and Kindle files."], workflows: [{ title: "Render covers", summary: "Choose archive paths, select output encoding, then render safely.", ui: ["Start with inspect.", "Enable write only after reviewing results."] }], commands: [{ title: "Inspect an archive", command: "xarcthumb inspect book.cbz --json", description: "Read the selected cover without writing a file.", examples: [{ label: "Write covers", command: "xarcthumb render library --output-dir D:/covers --write", description: "Write native encoded covers after review." }] }] } satisfies NodeHelp
