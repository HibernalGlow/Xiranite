/**
 * @migrated-from src/lib/components/viewer/VideoPlayer/VideoControls.svelte
 * @source-hash sha256:00ef7f489042b4c02415aa700ed5bd98c230a8c43c5546d583ed48e89ed11d30
 * @features 
 * @source-disposition converted
 * @migration-status scaffold
 * Generated by @xiranite/svelte-migrate; move to production only after behavior tests pass.
 */
import { Fragment } from "react"
import { Play, Pause, SkipBack, SkipForward, Repeat, Repeat1 } from 'lucide-react';
type LoopMode = 'none' | 'list' | 'single';
interface Props {
		isPlaying: boolean;
		loopMode: LoopMode;
		onTogglePlay: () => void;
		onSkipBackward: () => void;
		onSkipForward: () => void;
		onCycleLoopMode: () => void;
	}

export function VideoControls(props: Props) {
  const { isPlaying, loopMode, onTogglePlay, onSkipBackward, onSkipForward, onCycleLoopMode } = props
  return (
    <Fragment>

{/*  播放/暂停  */}
<button className="control-btn rounded-full p-2 transition-colors hover:bg-white/20" onClick={onTogglePlay} aria-label={isPlaying ? '暂停' : '播放'}>
	{isPlaying ? (<Fragment>
		<Pause className="text-primary h-6 w-6"></Pause>
	</Fragment>) : (<Fragment>
		<Play className="text-primary h-6 w-6"></Play>
	</Fragment>)}
</button>

{/*  快退  */}
<button className="control-btn rounded-full p-2 transition-colors hover:bg-white/20" onClick={onSkipBackward} aria-label="后退10秒">
	<SkipBack className="text-primary h-5 w-5"></SkipBack>
</button>

{/*  快进  */}
<button className="control-btn rounded-full p-2 transition-colors hover:bg-white/20" onClick={onSkipForward} aria-label="前进10秒">
	<SkipForward className="text-primary h-5 w-5"></SkipForward>
</button>

{/*  循环模式  */}
<button className="control-btn rounded-full p-2 transition-colors hover:bg-white/20" onClick={(event) => {
		event.stopPropagation();
		onCycleLoopMode();
	}} aria-label={loopMode === 'none' ? '不循环' : loopMode === 'single' ? '单个循环' : '列表循环'}>
	{loopMode === 'single' ? (<Fragment>
		<Repeat1 className="text-primary h-5 w-5"></Repeat1>
	</Fragment>) : (<Fragment>{loopMode === 'list' ? (<Fragment>
		<Repeat className="text-primary h-5 w-5"></Repeat>
	</Fragment>) : (<Fragment>
		<Repeat className="text-primary h-5 w-5 opacity-40"></Repeat>
	</Fragment>)}</Fragment>)}
</button></Fragment>
  )
}
