/**
 * @migrated-from src/lib/components/ui/tags-input/tags-input-tag.svelte
 * @source-hash sha256:c2b7925292bde274cbb9c9d5f8c3acfb2bc8c4078064753984cbd234c36f7f79
 * @features 
 * @source-disposition converted
 * @migration-status scaffold
 * Generated by @xiranite/svelte-migrate; move to production only after behavior tests pass.
 */
import { Fragment } from "react"
import XIcon from '@lucide/svelte/icons/x';
type Props = {
		value: string;
		disabled: boolean | null;
		active: boolean;
		onDelete: (value: string) => void;
	};

export function TagsInputTag(props: Props) {
  const { value, disabled, onDelete, active } = props
  return (
    <Fragment>

<div className="bg-secondary ring-offset-background hover:bg-secondary/90 aria-selected:bg-secondary/90 aria-selected:ring-ring flex place-items-center gap-2 rounded-md px-2 py-0.5 transition-all hover:cursor-default aria-selected:ring-2 aria-selected:ring-offset-2" aria-selected={active}>
	<span>
		{value}
	</span>
	<button type="button" disabled={disabled} onClick={() => onDelete(value)}>
		<XIcon className="size-4"></XIcon>
	</button>
</div></Fragment>
  )
}
