# GitAlso OpenTUI visual review

- GUI reference: `output/playwright/gitalso/gitalso-reference-review.jpg`
- Terminal capture: `artifacts/cli/gitalso/repository-deck.png`
- The workbench keeps the GUI's repository path, staged-file view, commit message, and diff/log output visible together.
- `diny` remains the normal commit-message path; GitButler stays an explicit fallback action in the existing CLI/core contract.
