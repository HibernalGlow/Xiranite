/* @jsxImportSource @opentui/react */
import { useKeyboard } from "@opentui/react"
import { useState } from "react"
import type { TerminalUiScreenProps } from "@xiranite/cli-runtime/terminal"
import { ActionTabs, ExecutionActions, ProgressBar, TerminalThemeProvider, WorkbenchField, WorkbenchPanel, resolveTerminalTheme, terminalIcon, useAnimation, useTerminalChromeActions, useTerminalTheme, useTerminalUiSession } from "@xiranite/cli-runtime/terminal/opentui"
import { createTerminalTranslator } from "@xiranite/cli-runtime/i18n"
import type { AudiovInput, AudiovResult } from "./core.js"
export function AudiovTui(p:TerminalUiScreenProps<AudiovInput,AudiovResult>){const [x]=useState(p.theme??p.preferences?.current.theme??"inherit");return <TerminalThemeProvider theme={resolveTerminalTheme(x==="inherit"?"nord":x)}><Lab {...p}/></TerminalThemeProvider>}
function Lab({definition,language,onExit}:TerminalUiScreenProps<AudiovInput,AudiovResult>){const th=useTerminalTheme(),t=createTerminalTranslator(language),s=useTerminalUiSession(definition),frame=useAnimation({intervalMs:s.phase==="running"?80:500}),data=s.result?.data,action=String(s.values.action??"plan");useTerminalChromeActions({onReset:s.reset,onExit});useKeyboard(k=>{if(k.name==="escape"){if(s.confirming)s.dismissConfirmation();else onExit()}});const f=(id:string)=>definition.schema.fields.find(x=>x.id===id)!;const Field=({id,width}:{id:string;width?:`${number}%`})=><box width={width} flexGrow={width?0:1}><WorkbenchField field={f(id)} value={s.values[id]} error={s.fieldErrors[id]} focused={s.focusedControlId===id} disabled={s.phase==="running"} t={t} onFocus={()=>s.focus(id)} onChange={v=>s.setField(id,v)}/></box>;return <box width="100%" height="100%" paddingLeft={1} paddingRight={1} flexDirection="column" overflow="hidden"><box height={4} flexShrink={0} borderStyle="single" borderColor={th.colors.border} paddingLeft={1} flexDirection="row" justifyContent="space-between"><box flexDirection="column"><text fg={th.colors.primary}><b>{`${terminalIcon("status")} AUDIOV // EXTRACTION DECK`}</b></text><text fg={th.colors.mutedForeground}>视频队列 · ffmpeg 命令预演 · 音轨输出</text></box><text fg={s.phase==="running"?th.colors.warning:th.colors.success}>{`${s.phase==="running"?"ENCODING":"DECK READY"} ${["·","•","●","•"][frame%4]}`}</text></box><box height={3} flexShrink={0} marginTop={1} flexDirection="row"><ActionTabs id="field-action" options={f("action").options??[]} value={action} focused={s.focusedControlId==="action"} disabled={s.phase==="running"} onFocus={()=>s.focus("action")} onChange={v=>s.setField("action",v)}/></box><box height={7} flexShrink={0} flexDirection="row" gap={1}><Field id="paths" width="58%"/><Field id="dryRun" width="16%"/><box width="23%" flexDirection="column" borderStyle="rounded" borderColor={s.dangerous?th.colors.error:th.colors.border} paddingLeft={1}><text fg={s.dangerous?th.colors.error:th.colors.mutedForeground}>{s.dangerous?"! WRITE M4A":"◌ COMMAND PLAN"}</text><box flexGrow={1}/><ExecutionActions session={s} executeLabel={action==="status"?"◌ 检查 ffmpeg":"⌕ 预演命令"} confirmLabel="! 开始提取"/></box></box><box flexGrow={1} minHeight={0} marginTop={1} flexDirection="row" gap={1}><WorkbenchPanel title="视频队列" description="待提取音轨的视频文件" width="30%"><scrollbox flexGrow={1}>{String(s.values.paths??"").split(/\r?\n/).filter(Boolean).map((p,i)=><text key={`${p}-${i}`}>{`▹ ${p}`}</text>)}</scrollbox></WorkbenchPanel><WorkbenchPanel title={`ffmpeg 计划 · ${data?.commands.length??0}`} description="固定 AAC / 192k / m4a 配置" width="40%"><scrollbox flexGrow={1}>{data?.commands.map((c,i)=><box key={`${c.inputPath}-${i}`} flexDirection="column"><text fg={th.colors.primary}>{`▶ ${c.label}`}</text><text fg={th.colors.mutedForeground}>{`${c.command} ${c.args.join(" ")}`}</text></box>)??<text fg={th.colors.mutedForeground}>预演后显示可复制的 ffmpeg 命令。</text>}</scrollbox></WorkbenchPanel><WorkbenchPanel title="输出与日志" description="结果文件、错误和实时进度" flexGrow={1}><scrollbox flexGrow={1}>{data?.outputPaths.map((p,i)=><text key={`${p}-${i}`} fg={th.colors.success}>{`✓ ${p}`}</text>)}{data?.errors.map((e,i)=><text key={`${e}-${i}`} fg={th.colors.error}>{`! ${e}`}</text>)}{s.logs.map((l,i)=><text key={`${l}-${i}`} fg={th.colors.mutedForeground}>{`${String(i+1).padStart(2,"0")} ${l}`}</text>)}</scrollbox></WorkbenchPanel></box><box height={3} flexShrink={0}><ProgressBar value={s.progress} label={s.status||s.preview[0]||"READY"}/></box></box>}
