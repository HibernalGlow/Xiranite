# TermUI showcase

Isolated evaluation of TermUI 0.1.7. This directory is outside the Xiranite
workspace and cannot enter the desktop/browser dependency graph.

```powershell
cd examples/termui-showcase
bun install
bun run start
```

The evaluation focuses on published-package compatibility, native mouse input,
forms, modal safety flows, theming, and the in-memory testing renderer.
