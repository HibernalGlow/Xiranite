/**
 * @migrated-from src/lib/components/viewer/VideoPlayer/PlaybackRatePanel.svelte
 * @source-hash sha256:00fc463164a2b2324273141b40bfb6453ec18eb354b837b0243690ae51657b5e
 * @features 
 * @source-disposition converted
 * @migration-status scaffold
 * Generated by @xiranite/svelte-migrate; move to production only after behavior tests pass.
 */
import { Fragment } from "react"
import { Gauge } from 'lucide-react';
interface Props {
		playbackRate: number;
		showRatePanel: boolean;
		minRate: number;
		maxRate: number;
		rateStep: number;
		onSetRate: (rate: number) => void;
		onSliderChange: (e: Event) => void;
		onTogglePanel: (e: MouseEvent) => void;
	}

export function PlaybackRatePanel(props: Props) {
  const {
		playbackRate,
		showRatePanel,
		minRate,
		maxRate,
		rateStep,
		onSetRate,
		onSliderChange,
		onTogglePanel
	} = props
  const presetRates = [0.5, 1, 1.5, 2];
  return (
    <Fragment>

<div className="relative">
	<button className={`control-btn flex items-center gap-1 rounded-full px-2 py-1.5 transition-colors hover:bg-white/20 ${showRatePanel
			? 'bg-white/20'
			: ''}`} onClick={onTogglePanel} aria-label="倍速控制" title="点击展开倍速调节">
		<Gauge className="text-primary h-4 w-4"></Gauge>
		<span className="text-primary text-xs">{playbackRate.toFixed(2)}x</span>
	</button>
	{showRatePanel ? (<Fragment>
		<div className="absolute right-0 bottom-full mb-2 rounded-lg bg-black/95 p-3 shadow-lg" style="min-width: 160px;" role="dialog" tabIndex="-1" onClick={(e) => e.stopPropagation()} onMouseDown={(e) => e.stopPropagation()} onKeyDown={(e) => e.stopPropagation()}>
			<div className="mb-2 flex items-center gap-2">
				<input type="range" min={minRate} max={maxRate} step={rateStep} value={playbackRate} onInput={onSliderChange} className="h-1 w-24 cursor-pointer appearance-none rounded bg-white/20" />
				<span className="shrink-0 text-xs text-white">{playbackRate.toFixed(2)}x</span>
			</div>
			<div className="flex flex-wrap gap-1">
				{presetRates.map((rate) => (<Fragment>
					<button className={`rounded px-2 py-0.5 text-xs ${playbackRate === rate
							? 'bg-primary text-white'
							: 'bg-white/10 text-white hover:bg-white/20'}`} onClick={() => onSetRate(rate)}>{rate}x</button>
				</Fragment>))}
			</div>
		</div>
	</Fragment>) : null}
</div></Fragment>
  )
}
