/**
 * @migrated-from src/lib/components/layout/TabBar.svelte
 * @source-hash sha256:30b55f8c0f89ede5c49cfe5c682548246b430dcd53963adaf9022cd418b900d6
 * @features panels-toolbar-shell
 * @source-disposition manual
 * @migration-status review-scaffold
 * Generated by @xiranite/svelte-migrate; move to production only after behavior tests pass.
 */
import { Fragment } from "react"
import { tabManager } from '$lib/core/tabs/tabManager';
import { Button } from '$lib/components/ui/button';
import { X, Plus } from 'lucide-react';

export function TabBar() {
  let tabs = $derived(tabManager.getAllTabs());

  let activeTab = $derived(tabManager.getActiveTab());

  async function handleCreateTab() {
      tabManager.createTab();
    }

  function handleSwitchTab(tabId: string) {
      tabManager.switchToTab(tabId);
    }

  function handleCloseTab(tabId: string, e: MouseEvent) {
      e.stopPropagation();
      tabManager.closeTab(tabId);
    }
  return (
    <Fragment>

<div className="bg-secondary/50 flex items-center gap-1 overflow-x-auto border-b px-2 py-1">
	{tabs.map((tab) => (<Fragment key={tab.id}>
		<button className={`group relative flex max-w-50 min-w-30 items-center gap-2 rounded-t-md px-3 py-1.5 text-sm transition-colors ${activeTab?.id === tab.id ? 'bg-background text-foreground' : 'bg-secondary/50 text-muted-foreground'}`} onClick={() => handleSwitchTab(tab.id)} title={tab.title}>
			<span className="flex-1 truncate text-left">{tab.title}</span>
			<div className="flex items-center gap-1 opacity-0 transition-opacity group-hover:opacity-100">
				{tabs.length > 1 ? (<Fragment>
					<Button variant="ghost" size="icon" className="hover:bg-destructive h-4 w-4" onClick={(e: MouseEvent) => handleCloseTab(tab.id, e)} title="关闭标签">
						<X className="h-3 w-3"></X>
					</Button>
				</Fragment>) : null}
			</div>
		</button>
	</Fragment>))}

	<Button variant="ghost" size="icon" className="h-7 w-7" onClick={handleCreateTab} title="新建标签">
		<Plus className="h-4 w-4"></Plus>
	</Button>
</div></Fragment>
  )
}
