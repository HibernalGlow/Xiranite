/**
 * @migrated-from src/lib/cards/benchmark/ImageSourceCard.svelte
 * @source-hash sha256:fbd64d0bace3d0af487a364158291cfecbb1e456220626f3ecb0b816bb3bfb8e
 * @features ai-ollama-translation,card-windows-tabs,file-browser-navigation,history-bookmarks-progress,input-bindings-radial-voice,metadata-dimensions-properties,page-index-navigation,page-layout-modes,panels-toolbar-shell,performance-benchmark-diagnostics,playlist-quick-library,search-sort-filter-library,settings-import-export-backup,startup-window-cli-lifecycle,super-resolution,theme-background-empty-state
 * @source-disposition manual
 * @migration-status partial-scaffold
 * @unsupported attribute BindDirective; template node ConstTag
 * Generated by @xiranite/svelte-migrate; move to production only after behavior tests pass.
 */
import { Fragment } from "react"
import { invoke } from '@tauri-apps/api/core';
import { convertFileSrc } from '@tauri-apps/api/core';
import { open } from '@tauri-apps/plugin-dialog';
import { Button } from '$lib/components/ui/button';
import { FolderOpen, Play, Trash2, Copy, Check, RotateCcw, Archive } from 'lucide-react';
interface TimingDetail {
		loadData: number; // 后端加载数据时间
		blobCreate: number; // Blob 创建时间
		urlCreate: number; // URL 创建时间
		imgDecode: number; // 图片解码时间
		total: number; // 总时间
	}
interface TestResult {
		mode: 'blob' | 'convertFileSrc';
		fileName: string;
		fileSize: number;
		dimensions: { width: number; height: number } | null;
		timing: TimingDetail;
		cached: boolean; // 是否为缓存命中
		success: boolean;
		error: string | null;
	}
interface TestSummary {
		mode: 'blob' | 'convertFileSrc';
		count: number;
		avgTotal: number;
		avgLoadData: number;
		avgBlobCreate: number;
		avgUrlCreate: number;
		avgImgDecode: number;
		minTotal: number;
		maxTotal: number;
		cacheHitRate: number;
	}
interface ImageInfo {
		archivePath: string;
		innerPath: string;
		displayName: string;
	}

export function ImageSourceCard() {
  class SimpleCache {
      private cache = new Map<string, { url: string; size: number }>();
      private maxSize = 100 * 1024 * 1024; // 100MB
      private currentSize = 0;

      has(key: string): boolean {
        return this.cache.has(key);
      }

      get(key: string): string | undefined {
        return this.cache.get(key)?.url;
      }

      set(key: string, url: string, size: number): void {
        // 简单 LRU：超出则清空
        if (this.currentSize + size > this.maxSize) {
          this.clear();
        }
        this.cache.set(key, { url, size });
        this.currentSize += size;
      }

      clear(): void {
        for (const [, item] of this.cache) {
          URL.revokeObjectURL(item.url);
        }
        this.cache.clear();
        this.currentSize = 0;
      }

      get size(): number {
        return this.cache.size;
      }
    }

  let selectedArchive = $state<string>('');

  let archiveImages = $state<ImageInfo[]>([]);

  let results = $state<TestResult[]>([]);

  let summary = $state<TestSummary[]>([]);

  let isRunning = $state(false);

  let iterations = $state<number>(3);

  let testCacheHit = $state(true);

  let copied = $state(false);

  let progress = $state({ current: 0, total: 0 });

  let testCount = $state<number>(5);

  const blobCache = new SimpleCache();

  const assetCache = new SimpleCache();

  async function selectArchive() {
      const file = await open({
        multiple: false,
        filters: [{ name: '压缩包', extensions: ['zip', 'cbz', 'rar', '7z', 'cb7', 'cbr'] }]
      });

      if (file && typeof file === 'string') {
        selectedArchive = file;
        results = [];
        summary = [];

        // 获取压缩包内图片列表
        try {
          const images = await invoke<string[]>('get_images_from_archive', {
            archivePath: file
          });
          archiveImages = images.slice(0, testCount).map((innerPath) => ({
            archivePath: file,
            innerPath,
            displayName: innerPath.split(/[/\\]/).pop() || innerPath
          }));
        } catch (err) {
          console.error('获取压缩包内容失败:', err);
          archiveImages = [];
        }
      }
    }

  function clearArchive() {
      selectedArchive = '';
      archiveImages = [];
      results = [];
      summary = [];
      blobCache.clear();
      assetCache.clear();
    }

  async function testBlobMode(img: ImageInfo, useCache: boolean): Promise<TestResult> {
      const cacheKey = `${img.archivePath}:${img.innerPath}`;
      const timing: TimingDetail = {
        loadData: 0,
        blobCreate: 0,
        urlCreate: 0,
        imgDecode: 0,
        total: 0
      };
      const totalStart = performance.now();

      try {
        let url: string;
        let fileSize = 0;
        let cached = false;

        // 检查缓存
        if (useCache && blobCache.has(cacheKey)) {
          url = blobCache.get(cacheKey)!;
          cached = true;
          timing.loadData = 0;
          timing.blobCreate = 0;
          timing.urlCreate = 0;
        } else {
          // 从压缩包加载原始数据
          const loadStart = performance.now();
          const rawData = await invoke<number[]>('load_image_from_archive', {
            archivePath: img.archivePath,
            filePath: img.innerPath
          });
          timing.loadData = performance.now() - loadStart;
          fileSize = rawData.length;

          // 创建 Blob
          const blobStart = performance.now();
          const blob = new Blob([new Uint8Array(rawData)]);
          timing.blobCreate = performance.now() - blobStart;

          // 创建 ObjectURL
          const urlStart = performance.now();
          url = URL.createObjectURL(blob);
          timing.urlCreate = performance.now() - urlStart;

          // 存入缓存
          if (useCache) {
            blobCache.set(cacheKey, url, fileSize);
          }
        }

        // 图片解码和加载
        const decodeStart = performance.now();
        const dimensions = await loadImageAndGetDimensions(url);
        timing.imgDecode = performance.now() - decodeStart;

        timing.total = performance.now() - totalStart;

        return {
          mode: 'blob',
          fileName: img.displayName,
          fileSize,
          dimensions,
          timing,
          cached,
          success: true,
          error: null
        };
      } catch (err) {
        timing.total = performance.now() - totalStart;
        return {
          mode: 'blob',
          fileName: img.displayName,
          fileSize: 0,
          dimensions: null,
          timing,
          cached: false,
          success: false,
          error: String(err)
        };
      }
    }

  async function testConvertFileSrcMode(img: ImageInfo, useCache: boolean): Promise<TestResult> {
      const cacheKey = `${img.archivePath}:${img.innerPath}`;
      const timing: TimingDetail = {
        loadData: 0,
        blobCreate: 0,
        urlCreate: 0,
        imgDecode: 0,
        total: 0
      };
      const totalStart = performance.now();

      try {
        let url: string;
        let cached = false;

        // 检查缓存
        if (useCache && assetCache.has(cacheKey)) {
          url = assetCache.get(cacheKey)!;
          cached = true;
          timing.loadData = 0;
          timing.urlCreate = 0;
        } else {
          // 解压到临时文件
          const extractStart = performance.now();
          const tempPath = await invoke<string>('extract_image_to_temp', {
            archivePath: img.archivePath,
            filePath: img.innerPath
          });
          timing.loadData = performance.now() - extractStart;

          // 创建 asset URL
          const urlStart = performance.now();
          url = convertFileSrc(tempPath);
          timing.urlCreate = performance.now() - urlStart;

          if (useCache) {
            assetCache.set(cacheKey, url, 0);
          }
        }

        // Blob 创建时间为 0（这种模式不创建 Blob）
        timing.blobCreate = 0;

        // 图片解码和加载
        const decodeStart = performance.now();
        const dimensions = await loadImageAndGetDimensions(url);
        timing.imgDecode = performance.now() - decodeStart;

        timing.total = performance.now() - totalStart;

        return {
          mode: 'convertFileSrc',
          fileName: img.displayName,
          fileSize: 0, // 临时文件模式不好获取大小
          dimensions,
          timing,
          cached,
          success: true,
          error: null
        };
      } catch (err) {
        timing.total = performance.now() - totalStart;
        return {
          mode: 'convertFileSrc',
          fileName: img.displayName,
          fileSize: 0,
          dimensions: null,
          timing,
          cached: false,
          success: false,
          error: String(err)
        };
      }
    }

  function loadImageAndGetDimensions(url: string): Promise<{ width: number; height: number }> {
      return new Promise((resolve, reject) => {
        const img = new Image();
        img.onload = () => resolve({ width: img.naturalWidth, height: img.naturalHeight });
        img.onerror = () => reject(new Error('图片加载失败'));
        img.src = url;
      });
    }

  function calculateSummary(mode: 'blob' | 'convertFileSrc'): TestSummary {
      const modeResults = results.filter((r) => r.mode === mode && r.success);
      if (modeResults.length === 0) {
        return {
          mode,
          count: 0,
          avgTotal: 0,
          avgLoadData: 0,
          avgBlobCreate: 0,
          avgUrlCreate: 0,
          avgImgDecode: 0,
          minTotal: 0,
          maxTotal: 0,
          cacheHitRate: 0
        };
      }

      const totals = modeResults.map((r) => r.timing.total);
      const cachedCount = modeResults.filter((r) => r.cached).length;

      return {
        mode,
        count: modeResults.length,
        avgTotal: totals.reduce((a, b) => a + b, 0) / totals.length,
        avgLoadData: modeResults.reduce((a, r) => a + r.timing.loadData, 0) / modeResults.length,
        avgBlobCreate: modeResults.reduce((a, r) => a + r.timing.blobCreate, 0) / modeResults.length,
        avgUrlCreate: modeResults.reduce((a, r) => a + r.timing.urlCreate, 0) / modeResults.length,
        avgImgDecode: modeResults.reduce((a, r) => a + r.timing.imgDecode, 0) / modeResults.length,
        minTotal: Math.min(...totals),
        maxTotal: Math.max(...totals),
        cacheHitRate: (cachedCount / modeResults.length) * 100
      };
    }

  async function runBenchmark() {
      if (archiveImages.length === 0) return;

      isRunning = true;
      results = [];
      summary = [];
      blobCache.clear();
      assetCache.clear();

      const totalTests = archiveImages.length * iterations * 2 * (testCacheHit ? 2 : 1);
      progress = { current: 0, total: totalTests };

      try {
        // 第一轮：冷启动测试（无缓存）
        for (let iter = 0; iter < iterations; iter++) {
          for (const img of archiveImages) {
            // Blob 模式
            const blobResult = await testBlobMode(img, false);
            results = [...results, blobResult];
            progress.current++;

            // ConvertFileSrc 模式
            const assetResult = await testConvertFileSrcMode(img, false);
            results = [...results, assetResult];
            progress.current++;

            // 清理缓存以确保每次都是冷启动
            blobCache.clear();
            assetCache.clear();
          }
        }

        // 第二轮：缓存命中测试
        if (testCacheHit) {
          // 先预热缓存
          for (const img of archiveImages) {
            await testBlobMode(img, true);
            await testConvertFileSrcMode(img, true);
          }

          // 测试缓存命中
          for (let iter = 0; iter < iterations; iter++) {
            for (const img of archiveImages) {
              const blobResult = await testBlobMode(img, true);
              results = [...results, blobResult];
              progress.current++;

              const assetResult = await testConvertFileSrcMode(img, true);
              results = [...results, assetResult];
              progress.current++;
            }
          }
        }

        // 计算摘要
        summary = [calculateSummary('blob'), calculateSummary('convertFileSrc')];
      } catch (err) {
        console.error('基准测试失败:', err);
      } finally {
        isRunning = false;
      }
    }

  function formatFileSize(bytes: number): string {
      if (bytes === 0) return '-';
      if (bytes < 1024) return `${bytes} B`;
      if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`;
      return `${(bytes / (1024 * 1024)).toFixed(2)} MB`;
    }

  function formatTime(ms: number): string {
      if (ms === 0) return '-';
      if (ms < 1) return `${(ms * 1000).toFixed(0)}µs`;
      return `${ms.toFixed(1)}ms`;
    }

  function generateCopyText(): string {
      if (summary.length === 0) return '';

      const lines: string[] = [];
      lines.push('=== 图像源模式基准测试 ===');
      lines.push(`测试时间: ${new Date().toLocaleString()}`);
      lines.push(`测试文件: ${archiveImages.length} 个 (${selectedArchive.split(/[/\\]/).pop()})`);
      lines.push(`迭代次数: ${iterations}`);
      lines.push(`缓存测试: ${testCacheHit ? '启用' : '禁用'}`);
      lines.push('');

      for (const s of summary) {
        const modeName = s.mode === 'blob' ? 'Blob 模式 (IPC)' : 'ConvertFileSrc 模式 (asset://)';
        lines.push(`--- ${modeName} ---`);
        lines.push(`  测试数: ${s.count}`);
        lines.push(`  平均总时间: ${formatTime(s.avgTotal)}`);
        lines.push(`  平均加载数据: ${formatTime(s.avgLoadData)}`);
        lines.push(`  平均创建 Blob: ${formatTime(s.avgBlobCreate)}`);
        lines.push(`  平均创建 URL: ${formatTime(s.avgUrlCreate)}`);
        lines.push(`  平均图片解码: ${formatTime(s.avgImgDecode)}`);
        lines.push(`  最快: ${formatTime(s.minTotal)} / 最慢: ${formatTime(s.maxTotal)}`);
        if (testCacheHit) {
          lines.push(`  缓存命中率: ${s.cacheHitRate.toFixed(0)}%`);
        }
        lines.push('');
      }

      // 对比
      if (summary.length === 2) {
        const blobSum = summary.find((s) => s.mode === 'blob')!;
        const assetSum = summary.find((s) => s.mode === 'convertFileSrc')!;
        const ratio = blobSum.avgTotal / assetSum.avgTotal;
        lines.push('--- 对比 ---');
        if (ratio > 1) {
          lines.push(`  ConvertFileSrc 比 Blob 快 ${ratio.toFixed(2)}x`);
        } else {
          lines.push(`  Blob 比 ConvertFileSrc 快 ${(1 / ratio).toFixed(2)}x`);
        }
      }

      return lines.join('\n');
    }

  async function copyResults() {
      const text = generateCopyText();
      if (text) {
        await navigator.clipboard.writeText(text);
        copied = true;
        setTimeout(() => (copied = false), 2000);
      }
    }

  function resetCaches() {
      blobCache.clear();
      assetCache.clear();
    }
  return (
    <Fragment>

<div className="space-y-3">
	<p className="text-muted-foreground text-[10px]">
		对比 Blob (IPC) 和 ConvertFileSrc (asset://) 加载模式
	</p>

	{/*  压缩包选择  */}
	<div className="flex gap-2">
		<Button onClick={selectArchive} variant="outline" size="sm" className="flex-1 text-xs">
			<Archive className="mr-1 h-3 w-3"></Archive>
			{selectedArchive ? `${archiveImages.length} 张` : '选择压缩包'}
		</Button>
		<Button onClick={runBenchmark} disabled={isRunning || archiveImages.length === 0} size="sm" className="flex-1 text-xs">
			<Play className="mr-1 h-3 w-3"></Play>
			{isRunning ? `${progress.current}/${progress.total}` : '运行测试'}
		</Button>
		{selectedArchive ? (<Fragment>
			<Button onClick={clearArchive} variant="ghost" size="sm" className="text-xs">
				<Trash2 className="h-3 w-3"></Trash2>
			</Button>
		</Fragment>) : null}
	</div>

	{/*  选项  */}
	<div className="flex flex-wrap items-center gap-3 text-[10px]">
		<div className="flex items-center gap-1">
			<span className="text-muted-foreground">图片:</span>
			<div className="flex gap-1">
				{[3, 5, 10, 20].map((count) => (<Fragment>
					<button type="button" className={`rounded px-1.5 py-0.5 ${testCount === count
							? 'bg-primary text-primary-foreground'
							: 'bg-muted hover:bg-muted/80'}`} onClick={() => (testCount = count)}>
						{count}
					</button>
				</Fragment>))}
			</div>
		</div>
		<div className="flex items-center gap-1">
			<span className="text-muted-foreground">迭代:</span>
			<div className="flex gap-1">
				{[1, 3, 5].map((count) => (<Fragment>
					<button type="button" className={`rounded px-1.5 py-0.5 ${iterations === count
							? 'bg-primary text-primary-foreground'
							: 'bg-muted hover:bg-muted/80'}`} onClick={() => (iterations = count)}>
						{count}
					</button>
				</Fragment>))}
			</div>
		</div>
		<label className="flex cursor-pointer items-center gap-1">
			<input type="checkbox" className="h-3 w-3" />
			<span className="text-muted-foreground">缓存</span>
		</label>
		<Button onClick={resetCaches} variant="ghost" size="sm" className="h-5 px-1 text-xs">
			<RotateCcw className="h-3 w-3"></RotateCcw>
		</Button>
	</div>

	{/*  压缩包信息  */}
	{selectedArchive ? (<Fragment>
		<div className="text-muted-foreground text-[10px]">
			<div className="truncate font-medium">{selectedArchive.split(/[/\\]/).pop()}</div>
			<div className="mt-1 max-h-16 space-y-0.5 overflow-auto">
				{archiveImages.map((img) => (<Fragment>
					<div className="truncate">{img.displayName}</div>
				</Fragment>))}
			</div>
		</div>
	</Fragment>) : null}

	{/*  摘要结果  */}
	{summary.length > 0 ? (<Fragment>
		<div className="space-y-2">
			<div className="flex items-center justify-between">
				<span className="text-[10px] font-medium">测试摘要</span>
				<Button onClick={copyResults} variant="ghost" size="sm" className="h-6 px-2">
					{copied ? (<Fragment>
						<Check className="h-3 w-3 text-green-500"></Check>
					</Fragment>) : (<Fragment>
						<Copy className="h-3 w-3"></Copy>
					</Fragment>)}
				</Button>
			</div>

			{summary.map((s) => (<Fragment>
				<div className="space-y-1 rounded border p-2 text-[10px]">
					<div className="flex justify-between font-medium">
						<span className={s.mode === 'blob' ? 'text-blue-500' : 'text-green-500'}>
							{s.mode === 'blob' ? 'Blob (IPC)' : 'ConvertFileSrc (asset://)'}
						</span>
						<span className="text-primary font-mono">{formatTime(s.avgTotal)}</span>
					</div>
					<div className="text-muted-foreground grid grid-cols-2 gap-x-4 gap-y-0.5">
						<div>
							加载: <span className="text-foreground font-mono">{formatTime(s.avgLoadData)}</span>
						</div>
						<div>
							Blob: <span className="text-foreground font-mono">{formatTime(s.avgBlobCreate)}</span>
						</div>
						<div>
							URL: <span className="text-foreground font-mono">{formatTime(s.avgUrlCreate)}</span>
						</div>
						<div>
							解码: <span className="text-foreground font-mono">{formatTime(s.avgImgDecode)}</span>
						</div>
					</div>
					<div className="text-muted-foreground flex justify-between border-t pt-1">
						<span>范围: {formatTime(s.minTotal)} ~ {formatTime(s.maxTotal)}</span>
						{testCacheHit ? (<Fragment>
							<span>缓存: {s.cacheHitRate.toFixed(0)}%</span>
						</Fragment>) : null}
					</div>
				</div>
			</Fragment>))}

			{/*  对比结论  */}
			{summary.length === 2 ? (<Fragment>
				{/* TODO unsupported ConstTag */}
				{/* TODO unsupported ConstTag */}
				{blobSum && assetSum && blobSum.avgTotal > 0 && assetSum.avgTotal > 0 ? (<Fragment>
					{/* TODO unsupported ConstTag */}
					<div className="rounded border p-2 text-center text-[10px]">
						{ratio > 1 ? (<Fragment>
							<span className="font-medium text-green-500">ConvertFileSrc</span>
							<span className="text-muted-foreground"> 比 </span>
							<span className="font-medium text-blue-500">Blob</span>
							<span className="text-muted-foreground"> 快 </span>
							<span className="text-primary font-mono font-medium">{ratio.toFixed(2)}x</span>
						</Fragment>) : (<Fragment>
							<span className="font-medium text-blue-500">Blob</span>
							<span className="text-muted-foreground"> 比 </span>
							<span className="font-medium text-green-500">ConvertFileSrc</span>
							<span className="text-muted-foreground"> 快 </span>
							<span className="text-primary font-mono font-medium">{(1 / ratio).toFixed(2)}x</span>
						</Fragment>)}
					</div>
				</Fragment>) : null}
			</Fragment>) : null}
		</div>
	</Fragment>) : null}

	{/*  详细结果（可折叠）  */}
	{results.length > 0 ? (<Fragment>
		<details className="text-[10px]">
			<summary className="text-muted-foreground hover:text-foreground cursor-pointer">
				详细结果 ({results.length} 条)
			</summary>
			<div className="mt-2 max-h-40 space-y-1 overflow-auto">
				{results.map((r) => (<Fragment>
					<div className="flex items-center justify-between rounded border px-2 py-1">
						<span className={r.mode === 'blob' ? 'text-blue-500' : 'text-green-500'}>
							{r.mode === 'blob' ? 'B' : 'A'}
							{r.cached ? (<Fragment><span className="ml-1 text-yellow-500">C</span></Fragment>) : null}
						</span>
						<span className="mx-2 flex-1 truncate">{r.fileName}</span>
						<span className={`font-mono ${r.success ? 'text-foreground' : 'text-red-500'}`}>
							{r.success ? formatTime(r.timing.total) : '失败'}
						</span>
					</div>
				</Fragment>))}
			</div>
		</details>
	</Fragment>) : null}
</div></Fragment>
  )
}
