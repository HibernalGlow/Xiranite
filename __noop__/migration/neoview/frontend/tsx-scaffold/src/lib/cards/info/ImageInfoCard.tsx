/**
 * @migrated-from src/lib/cards/info/ImageInfoCard.svelte
 * @source-hash sha256:decab0a74ce5a7df364bfa07940700f2a3ee889ee401cdb0f8de67ec30c45fe7
 * @features ai-ollama-translation,card-windows-tabs,file-browser-navigation,history-bookmarks-progress,input-bindings-radial-voice,metadata-dimensions-properties,page-index-navigation,page-layout-modes,panels-toolbar-shell,performance-benchmark-diagnostics,playlist-quick-library,search-sort-filter-library,settings-import-export-backup,startup-window-cli-lifecycle,super-resolution,theme-background-empty-state
 * @source-disposition manual
 * @migration-status review-scaffold
 * Generated by @xiranite/svelte-migrate; move to production only after behavior tests pass.
 */
import { Fragment } from "react"
import { infoPanelStore, type ViewerImageInfo } from '$lib/stores/infoPanel.js';

export function ImageInfoCard() {
  let imageInfo = $state<ViewerImageInfo | null>(null);

  $effect(() => {
      const unsubscribe = infoPanelStore.subscribe((state) => {
        imageInfo = state.imageInfo;
      });
      return unsubscribe;
    });

  function formatDuration(seconds?: number): string {
      if (seconds == null || seconds <= 0) return '—';
      const h = Math.floor(seconds / 3600);
      const m = Math.floor((seconds % 3600) / 60);
      const s = Math.floor(seconds % 60);
      if (h > 0) return `${h}:${m.toString().padStart(2, '0')}:${s.toString().padStart(2, '0')}`;
      return `${m}:${s.toString().padStart(2, '0')}`;
    }

  function formatBitrate(bps?: number): string {
      if (bps == null || bps <= 0) return '—';
      if (bps >= 1_000_000) return `${(bps / 1_000_000).toFixed(1)} Mbps`;
      if (bps >= 1_000) return `${(bps / 1_000).toFixed(0)} Kbps`;
      return `${bps} bps`;
    }

  function formatFileSize(bytes?: number): string {
      if (bytes == null || bytes <= 0) return '—';
      if (bytes >= 1_073_741_824) return `${(bytes / 1_073_741_824).toFixed(2)} GB`;
      if (bytes >= 1_048_576) return `${(bytes / 1_048_576).toFixed(2)} MB`;
      if (bytes >= 1_024) return `${(bytes / 1_024).toFixed(1)} KB`;
      return `${bytes} B`;
    }
  return (
    <Fragment>

{imageInfo ? (<Fragment>
	<div className="space-y-2 text-sm">
		{/*  媒体类型  */}
		<div className="flex items-center justify-between">
			<span className="text-muted-foreground">类型:</span>
			<span className="flex items-center gap-1">
				{imageInfo.isVideo ? (<Fragment>
					<span>🎬</span> 视频
				</Fragment>) : (<Fragment>
					<span>🖼️</span> 图片
				</Fragment>)}
			</span>
		</div>

		{/*  文件名  */}
		<div className="flex justify-between">
			<span className="text-muted-foreground">文件名:</span>
			<span className="max-w-[150px] truncate font-mono text-xs" title={imageInfo.name}>
				{imageInfo.name}
			</span>
		</div>

		{/*  尺寸  */}
		<div className="flex justify-between">
			<span className="text-muted-foreground">尺寸:</span>
			<span>
				{imageInfo.width && imageInfo.height ? (<Fragment>
					{imageInfo.width} × {imageInfo.height}
				</Fragment>) : (<Fragment>
					—
				</Fragment>)}
			</span>
		</div>

		{imageInfo.isVideo ? (<Fragment>
			{/*  视频特有信息  */}
			<div className="flex justify-between">
				<span className="text-muted-foreground">时长:</span>
				<span>{formatDuration(imageInfo.duration)}</span>
			</div>
			{imageInfo.frameRate ? (<Fragment>
				<div className="flex justify-between">
					<span className="text-muted-foreground">帧率:</span>
					<span>{imageInfo.frameRate.toFixed(0)} fps</span>
				</div>
			</Fragment>) : null}
			{imageInfo.bitrate ? (<Fragment>
				<div className="flex justify-between">
					<span className="text-muted-foreground">码率:</span>
					<span>{formatBitrate(imageInfo.bitrate)}</span>
				</div>
			</Fragment>) : null}
			{imageInfo.videoCodec ? (<Fragment>
				<div className="flex justify-between">
					<span className="text-muted-foreground">视频编码:</span>
					<span>{imageInfo.videoCodec}</span>
				</div>
			</Fragment>) : null}
			{imageInfo.audioCodec ? (<Fragment>
				<div className="flex justify-between">
					<span className="text-muted-foreground">音频编码:</span>
					<span>{imageInfo.audioCodec}</span>
				</div>
			</Fragment>) : null}
		</Fragment>) : (<Fragment>
			{/*  图片特有信息  */}
			<div className="flex justify-between">
				<span className="text-muted-foreground">格式:</span>
				<span>{imageInfo.format ?? '—'}</span>
			</div>
			{imageInfo.fileSize ? (<Fragment>
				<div className="flex justify-between">
					<span className="text-muted-foreground">大小:</span>
					<span>{formatFileSize(imageInfo.fileSize)}</span>
				</div>
			</Fragment>) : null}
		</Fragment>)}
	</div>
</Fragment>) : (<Fragment>
	<div className="text-muted-foreground py-2 text-center text-sm">暂无媒体信息</div>
</Fragment>)}</Fragment>
  )
}
