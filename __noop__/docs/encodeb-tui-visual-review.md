# EncodeB TUI visual review

- GUI reference: `output/playwright/encodeb/encodeb-reference-review.jpg`
- PTY capture: `artifacts/cli/encodeb/recovery-lab.png`

The terminal workbench follows the GUI's source/strategy/result relationship:
paths on the left, encoding strategy and animated analysis centre, and a
scrollable `src → dst` repair mapping on the right. Find and preview are
read-only; recover remains confirmation-gated.
