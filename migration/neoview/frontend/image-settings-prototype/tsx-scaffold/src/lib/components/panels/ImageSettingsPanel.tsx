/**
 * @migrated-from src/lib/components/panels/ImageSettingsPanel.svelte
 * @source-hash sha256:41baf40949e686ede6cb5b2b929f91d753fca2251cce128caea3d60bb4982ff9
 * @features settings-import-export-backup
 * @source-disposition manual
 * @migration-status partial-scaffold
 * @unsupported attribute BindDirective
 * Generated by @xiranite/svelte-migrate; move to production only after behavior tests pass.
 */
import { Fragment } from "react"
import {
		Palette,
		Image as ImageIcon,
		Video,
		MousePointer,
		Check,
		X,
		RefreshCw
	} from 'lucide-react';
import { Button } from '$lib/components/ui/button';
import { settingsManager } from '$lib/settings/settingsManager';
import { Label } from '$lib/components/ui/label';
import { Switch } from '$lib/components/ui/switch';
import Checkbox from '$lib/components/ui/checkbox/checkbox.js';
import * as Tabs from '$lib/components/ui/tabs';

export function ImageSettingsPanel() {
  let activeTab = $state('image');

  let currentSettings = $state(settingsManager.getSettings());

  settingsManager.addListener((s) => {
      currentSettings = s;
    });

  function toggleFormat(fmt: string) {
      const formats = new Set(currentSettings.image.supportedFormats);
      if (formats.has(fmt)) {
        formats.delete(fmt);
      } else {
        formats.add(fmt);
      }
      settingsManager.updateNestedSettings('image', {
        supportedFormats: Array.from(formats)
      });
    }

  let testStatus = $state<'idle' | 'testing' | 'supported' | 'unsupported'>('idle');

  async function testJxlSupport() {
      testStatus = 'testing';
      const jxl = new Image();
      // 一个最小的合法 JXL (1x1 无损白点)
      jxl.src = 'data:image/jxl;base64,/xl/AAAABQAAABAAAAEBAQA=';

      try {
        await new Promise((resolve, reject) => {
          jxl.onload = () => resolve(true);
          jxl.onerror = (e) => reject(e);
          // 3秒超时
          setTimeout(() => reject(new Error('Timeout')), 3000);
        });
        testStatus = 'supported';
      } catch (e) {
        console.error('JXL test failed:', e);
        testStatus = 'unsupported';
      }
    }
  return (
    <Fragment>

<div className="space-y-4 p-6">
	<div className="space-y-2">
		<h3 className="flex items-center gap-2 text-lg font-semibold">
			<Palette className="h-5 w-5"></Palette>
			影像综合管理
		</h3>
		<p className="text-muted-foreground text-sm">配置全局图片和视频的加载与播放行为</p>
	</div>

	<Tabs.Root className="w-full">
		<Tabs.List className="grid w-full grid-cols-3">
			<Tabs.Trigger value="image" className="gap-1.5 text-xs">
				<ImageIcon className="h-3.5 w-3.5"></ImageIcon>
				图片
			</Tabs.Trigger>
			<Tabs.Trigger value="video" className="gap-1.5 text-xs">
				<Video className="h-3.5 w-3.5"></Video>
				视频
			</Tabs.Trigger>
			<Tabs.Trigger value="interaction" className="gap-1.5 text-xs">
				<MousePointer className="h-3.5 w-3.5"></MousePointer>
				交互
			</Tabs.Trigger>
		</Tabs.List>

		<Tabs.Content value="image" className="mt-4 space-y-4">
			{/*  动画图片（GIF / APNG）自动播放  */}
			<div className="space-y-3">
				<div className="flex items-center justify-between gap-2">
					<Label className="text-sm">自动播放 GIF / APNG</Label>
					<Switch checked={currentSettings.image.autoPlayAnimatedImages} onCheckedChange={(checked) =>
							settingsManager.updateNestedSettings('image', {
								autoPlayAnimatedImages: !!checked
							})}></Switch>
				</div>
				<p className="text-muted-foreground text-xs">
					关闭后，GIF / APNG 等动图将以静态首帧显示，需手动触发播放（实现后）。
				</p>
			</div>

			{/*  支持的图片格式  */}
			<div className="space-y-2">
				<h4 className="text-sm font-semibold">支持的图片格式</h4>
				<div className="grid gap-2 sm:grid-cols-2">
					<label className="flex items-center gap-2 text-sm">
						<Checkbox checked={currentSettings.image.supportedFormats.includes('jpg')} onClick={() => toggleFormat('jpg')} aria-label="JPG/JPEG"></Checkbox>
						<span>JPG / JPEG</span>
					</label>
					<label className="flex items-center gap-2 text-sm">
						<Checkbox checked={currentSettings.image.supportedFormats.includes('webp')} onClick={() => toggleFormat('webp')} aria-label="WebP"></Checkbox>
						<span>WebP</span>
					</label>
					<label className="flex items-center gap-2 text-sm">
						<Checkbox checked={currentSettings.image.supportedFormats.includes('jxl')} onClick={() => toggleFormat('jxl')} aria-label="JXL"></Checkbox>
						<span>JXL</span>
					</label>
				</div>
				<p className="text-muted-foreground text-xs">控制 NeoView 默认识别为图片的文件扩展名。</p>
			</div>

			{/*  原生 JXL 解码  */}
			<div className="space-y-3">
				<div className="flex items-center justify-between gap-2">
					<div className="flex flex-col gap-0.5">
						<Label className="text-sm">原生 JXL 解码</Label>
						<div className="mt-1 flex items-center gap-2">
							<Button variant="outline" size="sm" className="h-7 px-2 text-[10px]" onClick={testJxlSupport} disabled={testStatus === 'testing'}>
								{testStatus === 'testing' ? (<Fragment>
									<RefreshCw className="mr-1 h-3 w-3 animate-spin"></RefreshCw>
									检测中...
								</Fragment>) : (<Fragment>
									检测浏览器支持
								</Fragment>)}
							</Button>

							{testStatus === 'supported' ? (<Fragment>
								<span className="flex items-center gap-1 text-[10px] text-green-500">
									<Check className="h-3 w-3"></Check> 支持原生解析
								</span>
							</Fragment>) : (<Fragment>{testStatus === 'unsupported' ? (<Fragment>
								<span className="text-destructive flex items-center gap-1 text-[10px]">
									<X className="h-3 w-3"></X> 浏览器不支持
								</span>
							</Fragment>) : null}</Fragment>)}
						</div>
					</div>
					<Switch checked={currentSettings.image.nativeJxl ?? false} onCheckedChange={async (checked) => {
							const val = !!checked;
							settingsManager.updateNestedSettings('image', { nativeJxl: val });
							try {
								const { invoke } = await import('@tauri-apps/api/core');
								await invoke('update_startup_config_field', {
									field: 'nativeJxl',
									value: val ? 'true' : null
								});
							} catch (e) {
								console.warn('更新启动配置失败:', e);
							}
						}}></Switch>
				</div>
				<p className="text-muted-foreground text-xs">
					使用 WebView2 (Chromium 145+) 内置的 JXL 解码器直接渲染，跳过 Rust 端的 JXL→PNG
					转码，性能更优。<br />
					<span className="font-medium text-yellow-500">⚠ 需要 WebView2 Runtime ≥ 145 / 重启后生效</span>
				</p>
			</div>
		</Tabs.Content>

		<Tabs.Content value="video" className="mt-4 space-y-4">
			<div className="space-y-2">
				<h4 className="text-sm font-semibold">支持的视频格式</h4>
				<input type="text" className="bg-background w-full rounded border px-2 py-1 text-sm" placeholder="mp4, webm, mkv, flv, ..." value={currentSettings.image.videoFormats?.join(', ') ?? ''} onChange={(event) => {
						const target = event.target as HTMLInputElement;
						const raw = target.value || '';
						const parts = raw
							.split(',')
							.map((s) => s.trim())
							.filter((s) => s.length > 0);
						settingsManager.updateNestedSettings('image', {
							videoFormats: parts
						});
					}} />
				<p className="text-muted-foreground text-xs">
					使用逗号分隔扩展名（不区分大小写，可选带或不带点），用于判断文件是否作为视频打开。
				</p>
			</div>

			{/*  视频倍速范围  */}
			<div className="space-y-2">
				<h4 className="text-sm font-semibold">视频倍速范围</h4>
				<div className="space-y-2">
					<div className="flex items-center justify-between gap-2">
						<Label className="text-sm">最小倍速</Label>
						<input type="number" min="0.05" step="0.05" value={currentSettings.image.videoMinPlaybackRate} className="bg-background w-24 rounded border px-2 py-1 text-right text-sm" onChange={(event) => {
								const target = event.target as HTMLInputElement;
								const raw = parseFloat(target.value);
								const max = currentSettings.image.videoMaxPlaybackRate;
								const value = Number.isNaN(raw) ? 0.25 : raw;
								const clamped = Math.max(0.05, Math.min(value, max));
								settingsManager.updateNestedSettings('image', {
									videoMinPlaybackRate: clamped
								});
							}} />
					</div>
					<div className="flex items-center justify-between gap-2">
						<Label className="text-sm">最大倍速</Label>
						<input type="number" min={currentSettings.image.videoMinPlaybackRate} step="0.25" value={currentSettings.image.videoMaxPlaybackRate} className="bg-background w-24 rounded border px-2 py-1 text-right text-sm" onChange={(event) => {
								const target = event.target as HTMLInputElement;
								const raw = parseFloat(target.value);
								const min = currentSettings.image.videoMinPlaybackRate;
								const value = Number.isNaN(raw) ? 16 : raw;
								const clamped = Math.max(min, Math.min(value, 64));
								settingsManager.updateNestedSettings('image', {
									videoMaxPlaybackRate: clamped
								});
							}} />
					</div>
					<div className="flex items-center justify-between gap-2">
						<Label className="text-sm">调节步长</Label>
						<input type="number" min="0.01" step="0.01" value={currentSettings.image.videoPlaybackRateStep} className="bg-background w-24 rounded border px-2 py-1 text-right text-sm" onChange={(event) => {
								const target = event.target as HTMLInputElement;
								const raw = parseFloat(target.value);
								const value = Number.isNaN(raw) ? 0.25 : raw;
								const clamped = Math.max(0.01, Math.min(value, 4));
								settingsManager.updateNestedSettings('image', {
									videoPlaybackRateStep: clamped
								});
							}} />
					</div>
				</div>
			</div>
		</Tabs.Content>

		<Tabs.Content value="interaction" className="mt-4 space-y-4">
			{/*  悬停滚动倍率  */}
			<div className="space-y-2">
				<h4 className="text-sm font-semibold">悬停滚动倍率</h4>
				<div className="flex items-center justify-between gap-2">
					<Label className="text-sm">滚动速度</Label>
					<input type="number" min="0.5" max="10" step="0.5" value={currentSettings.image.hoverScrollSpeed ?? 2.0} className="bg-background w-24 rounded border px-2 py-1 text-right text-sm" onChange={(event) => {
							const target = event.target as HTMLInputElement;
							const raw = parseFloat(target.value);
							const value = Number.isNaN(raw) ? 2.0 : raw;
							const clamped = Math.max(0.5, Math.min(value, 10));
							settingsManager.updateNestedSettings('image', {
								hoverScrollSpeed: clamped
							});
						}} />
				</div>
				<p className="text-muted-foreground text-xs">
					控制鼠标悬停滚动的速度倍率（0.5-10），数值越大滚动越快。
				</p>
			</div>
		</Tabs.Content>
	</Tabs.Root>
</div></Fragment>
  )
}
