/* @jsxImportSource @opentui/react */
import{testRender}from"@opentui/react/test-utils";import{expect,test}from"bun:test";import{act}from"react";import{createDissolvefInteractionSchema}from"./interaction.js";import{DissolvefTui}from"./Tui.js";test("DissolveF renders folder flux and launches history",async()=>{let runs=0;const schema=createDissolvefInteractionSchema({path:"D:/root"},"zh"),x=await testRender(<DissolvefTui definition={{schema,run:async()=>{runs++;return{success:true,message:"ok",data:{plan:[],history:[],archivePaths:[],nestedCount:0,mediaCount:0,archiveCount:0,directFiles:0,directDirs:0,skippedCount:0,totalCount:0,successCount:0,failedCount:0,errorCount:0,operationId:"",errors:[]}}}}} language="zh" onExit={()=>undefined}/>,{width:142,height:40,useMouse:true});try{await act(async()=>x.renderOnce());const f=x.captureCharFrame();expect(f).toContain("DISSOLVEF // FOLDER FLUX");expect(f).toContain("操作流");expect(f).toContain("冲突与相似度");const h=x.renderer.root.findDescendantById("history-action-history");expect(h).toBeDefined();await act(async()=>x.mockMouse.click(h!.x+2,h!.y+1));await x.waitFor(()=>runs===1);expect(runs).toBe(1)}finally{await act(async()=>x.renderer.destroy())}})
